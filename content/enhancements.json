{
  "note": "The machine-readable half of the enhancement law (Capsid: progressive-enhancement.md). Hard rule 9: every enhancement on a public reading route declares a NAMED fallback, even when the fallback is nothing. Every module here is bundled by build:enhance; the public plane does not hydrate React (since 2026-08-26), so these bundles are the whole public script payload. Three of them are loaded by a nonced module script tag rendered beside the markup they upgrade. The fourth, the command palette, is loaded ON THE FIRST GESTURE by theme.ts, which appends a script element for it: it is the largest bundle on the public plane and it answers one gesture, so from 2026-08-27 the readers who never open it never download it. A script element inserted by an already-trusted script is allowed by script-src 'strict-dynamic' with no nonce of its own. Gated by check:features, which asserts that every file in app/enhance/ appears here, that every entry names a fallback, and that every named selector or route actually exists. It does NOT assert the prose is true, and it cannot see the wire: whether a route is genuinely server-complete with script off is verify-live's job, not a gate's.",
  "fallbackKinds": {
    "route": "The fallback is a server-rendered route. It must be declared in app/routes.ts.",
    "selector": "The fallback is server-rendered markup. Every identifier in the selector must appear in app/ OUTSIDE app/enhance/, because markup an enhancement creates for itself is not a fallback.",
    "none": "There is deliberately no fallback markup. Requires a why, because an unwritten nothing is an omission and a written one is a decision."
  },
  "enhancements": [
    {
      "id": "reading-progress",
      "module": "app/enhance/blog.ts",
      "enhancement": "A scroll-linked bar across the top of a post showing how far through it you are.",
      "fallback": "Nothing. The bar is absent and the post is unchanged.",
      "fallbackKind": "none",
      "fallbackAt": null,
      "why": "It is decoration and carries no information the post does not. Writing the nothing down is what makes it a decision rather than an omission."
    },
    {
      "id": "toc-scrollspy",
      "module": "app/enhance/blog.ts",
      "enhancement": "Highlights the table of contents entry for the section currently on screen.",
      "fallback": "The table of contents is server-rendered anchor links and navigates either way. Only the highlight is lost.",
      "fallbackKind": "selector",
      "fallbackAt": ".post-toc",
      "why": null
    },
    {
      "id": "code-copy-and-label",
      "module": "app/enhance/blog.ts",
      "enhancement": "Adds a copy button and a language label to each code block.",
      "fallback": "The code is already fully highlighted by the build pipeline, including highlighted line ranges, and is selectable. Only the button is lost.",
      "fallbackKind": "selector",
      "fallbackAt": "pre[data-lang]",
      "why": null
    },
    {
      "id": "heading-copy-link",
      "module": "app/enhance/blog.ts",
      "enhancement": "Turns each heading's autolink into a click-to-copy for the section URL.",
      "fallback": "The heading autolinks are ordinary anchors and are already navigable and linkable.",
      "fallbackKind": "selector",
      "fallbackAt": ".heading-anchor",
      "why": null
    },
    {
      "id": "footnote-previews",
      "module": "app/enhance/blog.ts",
      "enhancement": "Shows a footnote's text in a hover preview card without leaving the paragraph.",
      "fallback": "Footnotes are ordinary bidirectional links. The jump down and the jump back both work.",
      "fallbackKind": "selector",
      "fallbackAt": ".prose .footnotes",
      "why": null
    },
    {
      "id": "image-lightbox",
      "module": "app/enhance/blog.ts",
      "enhancement": "Intercepts a click on a post image and opens it full size in an overlay.",
      "fallback": "The image is a link to its original file; clicking it opens the original. Only the overlay is lost.",
      "fallbackKind": "selector",
      "fallbackAt": ".image-link",
      "why": null
    },
    {
      "id": "copy-as-markdown",
      "module": "app/enhance/blog.ts",
      "enhancement": "Upgrades the markdown link into a clipboard copy, keeping the link intact.",
      "fallback": "The control is an ordinary anchor to the post's .md twin, which is a real route and serves the source.",
      "fallbackKind": "route",
      "fallbackAt": "/blog/:slug.md",
      "why": null
    },
    {
      "id": "copy-link-to-selection",
      "module": "app/enhance/blog.ts",
      "enhancement": "Offers a link to the passage a reader has selected, as a URL text fragment, in the flow after the selected block.",
      "fallback": "The permalink, which links to the whole post rather than to the passage. It is server-rendered and always present.",
      "fallbackKind": "selector",
      "fallbackAt": ".post-share",
      "why": "There is no scriptless way to ask a reader what they highlighted, so the fallback cannot be this affordance degraded; it is the nearest thing the page can always offer. The permalink is deliberately not a clipboard button, so this adds a capability rather than a second copy of one."
    },
    {
      "id": "theme-toggle",
      "module": "app/enhance/theme.ts",
      "enhancement": "Applies the theme in place, without a round trip. Also holds the \"/\" key, the Cmd-K chord and the search trigger's click, and fetches the command palette bundle on the first of them.",
      "fallback": "The control is a POST form to /theme. The action sets the cookie and the server renders the choice into the first byte. Both paths end at the same cookie. The control ships both of its buttons and CSS displays the one matching the theme in effect, so the visible button posts the right value with no script at all.",
      "fallbackKind": "route",
      "fallbackAt": "/theme",
      "why": null
    },
    {
      "id": "command-palette",
      "module": "app/enhance/palette.ts",
      "enhancement": "Upgrades the header's search link into a modal command palette with live results.",
      "fallback": "The header ships an ordinary anchor to /search, which is a full server-rendered search page. If the bundle fails to load, fails to parse, or throws on its first line, the anchor is still an anchor. Since the bundle is fetched on the gesture rather than with the page, a fetch that fails navigates to that same /search rather than leaving the gesture unanswered.",
      "fallbackKind": "route",
      "fallbackAt": "/search",
      "why": null
    },
    {
      "id": "ask-answers",
      "module": "app/enhance/ask.ts",
      "enhancement": "Streams a generated answer with citations above the classic results, on surfaces that already rendered them.",
      "fallback": "/search is exactly what it was before Layer 2: classic server-rendered results. The Ask button ships hidden and is unhidden by the bundle, and the bundle's script tag is rendered only when the server said the binding exists and the query is a real question.",
      "fallbackKind": "route",
      "fallbackAt": "/search",
      "why": null
    },
    {
      "id": "search-live",
      "module": "app/enhance/search.ts",
      "enhancement": "Replaces the results list in place as the reader types, after three characters, by re-requesting the SAME URL the form would have submitted at Accept: application/json. Also moves focus through the results with the arrow keys and clears the field on Escape. No client index and no model call.",
      "fallback": "The page is a GET form whose response is the answer. Pressing Enter submits it and the server renders the same results this module fetches, from the same query against the same index.",
      "fallbackKind": "selector",
      "fallbackAt": ".search-form",
      "why": null
    }
  ]
}
