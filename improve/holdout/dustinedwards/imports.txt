# Every name the hidden holdout suite imports out of this repo's source.
#
# NAMES ONLY, one per line. Committed and deliberately not secret: an export name
# is already in the source, so publishing the list tells a reader nothing the
# source does not. What is secret is the suite itself, and no part of it is here.
#
# WHY THIS FILE EXISTS. On 2026-09-07 a bloat pass in capsid-mcp removed two
# exports on a scan that found no caller in src/ or test/. The holdout imported
# both, and the holdout is structurally invisible to anything that runs in the
# repo: that is its entire point. 28 of 30 against an anchor of min 1.0, and
# nothing local could have predicted it.
#
# Enforced in BOTH directions: a dead-export check treats a name here as a
# caller, and the scorer's Job B refuses a holdout case importing a name that is
# NOT here.
#
# Harvested from the first run of that gate on 2026-09-08, which reports every
# name it finds. An EXTRA name here costs nothing; a missing one is a red gate.

ALL_WIDTHS
CONTENT_SIZES
FAILED_RETENTION_DAYS
REJECTED_RETENTION_DAYS
THUMB_WIDTHS
apca
channels
contentSrcSet
contrast
countWords
daysInMilliseconds
folderFor
luminance
minutesForWords
normalizeHex
readingTimeMinutes
tagPath
