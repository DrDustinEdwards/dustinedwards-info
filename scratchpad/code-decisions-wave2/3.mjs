// Chunk 3: scripts/check-page-payload.mjs, blocks 0-100.
//
// Re-cut to wave 1's rule, the same pass chunk 4 records: one line a block, the because as a
// clause, separators keep their label and lose the dashes, and the justification for choosing
// an approach is deleted rather than compressed.
//
// This file's own concentration is the eleven dated "MEASURED <date> through this gate on a
// fresh build" openings. Every ceiling they justify is the constant directly under the comment,
// so the measurement goes and what stays is which bar the route is graded against.
export default {
  "scripts/check-page-payload.mjs#0": [
    "CONTRACT",
    "the boundary, the byte-equality rule and the syntax pass; both rename narratives, the dated plant and the whole-page navigation go to the history document",
    `Gate: what a reader downloads to see a public page, per route, with ceilings.

  npm run check:page-payload

It reads the BUILD ON DISK and never builds, so against a stale build it certifies the stale
build; it cannot see a RENDERED page, which is check:browser's half and verify-live's.
Enhancement assets are matched by BYTE EQUALITY, never by name, because a Vite hash may
contain a dash. Every .js asset goes through \`node --check\`, since a ?url import pointed at
the .ts SOURCE ships TypeScript that parses nowhere.`,
  ],
  "scripts/check-page-payload.mjs#1": ["WHY", "the comment-satisfies-a-gate trap; two lines already"],
  "scripts/check-page-payload.mjs#2": [
    "WHY",
    "why the resolution is a pure module, in one line",
    `Pure and therefore testable, the footing ci-status.mjs and ask-converge.mjs stand on.`,
  ],
  "scripts/check-page-payload.mjs#3": [
    "NUMBER",
    "the margin discipline and the missing-module rule; the four dated bundle measurements go to the history document",
    `CEILINGS AND FLOORS. The only copies of these numbers, rule 17.

Margins are roughly fifty percent over measured: the bundles are tiny, so the job is catching
a dependency wandering in rather than a feature growing. A module missing from this map fails.

@type {Record<string, number>}`,
  ],
  "scripts/check-page-payload.mjs#4": [
    "NUMBER",
    "what a low count means; the dated re-measurement goes to the history document",
    `A walk that reads fewer files than this has lost a route or gone vacuous, not gotten lean.`,
  ],
  "scripts/check-page-payload.mjs#5": ["WHY", "one line already; kept"],
  "scripts/check-page-payload.mjs#7": [
    "CONTRACT",
    "why stems and why the pattern anchors on the extension; the example spelling and the verify-live rationale go out",
    `A chunk name with its content hash stripped. Stems rather than full names, because a
standalone verify-live run may face a deploy whose hashes predate this disk's build. Anchored
on the extension, since a Vite hash may itself contain a dash.

@param {string} name`,
  ],
  "scripts/check-page-payload.mjs#9": [
    "CONTRACT",
    "the fail-closed enumeration, in one line",
    `Fails closed in every direction: no build, no manifest, two manifests, or one that does not
parse are all throws, never an empty walk.`,
  ],
  "scripts/check-page-payload.mjs#11": [
    "CONTRACT",
    "the three minified spellings, which is why the regexes look as they do",
    `Minified output writes \`from"./x.js"\`, bare \`import"./x.js"\` and \`import("./x.js")\`, any of
the three quote characters.

@param {string} source
@returns {{ static: string[], dynamic: string[] }}`,
  ],
  "scripts/check-page-payload.mjs#14": ["WHY", "what the lookbehind refuses; two lines already"],
  "scripts/check-page-payload.mjs#15": [
    "CONTRACT",
    "what the set is and why it survives; the tense-bound 'no public page references it any more' made plain",
    `The set the framework WOULD hand a hydrating page, kept as a structural floor on the manifest
and for verify-live's stem comparison. No public page references it; the admin plane and
/login do.

@returns {{ files: string[], dynamicTargets: Set<string>, manifestFile: string }}`,
  ],
  "scripts/check-page-payload.mjs#18": ["WHY", "why the walk re-closes transitively; two lines already"],
  // Kept byte-identical rather than trimmed: its @returns line is already at the 110-column
  // limit, and the type cannot wrap without the JSDoc head resolving to a bare @returns.
  "scripts/check-page-payload.mjs#21": ["CONTRACT", "the byte-equality rule and what verify-live reads; already short"],
  "scripts/check-page-payload.mjs#23": [
    "WHY",
    "the one-walker discipline in one line; the hoist date goes to the history document",
    `ONE WALKER, ONE ARGUMENT ORDER, which the helper-signature line of hard rule 10 names: two
copies agree until one gains an extension.

@param {string} dir
@returns {string[]} absolute paths`,
  ],
  "scripts/check-page-payload.mjs#25": ["CONTRACT", "section marker, rule padding cut", `every bundle is served verbatim, and its ceiling holds`],
  "scripts/check-page-payload.mjs#26": ["CONTRACT", "section marker, rule padding cut", `the syntax pass: every served .js asset actually parses`],
  "scripts/check-page-payload.mjs#27": [
    "WHY",
    "why the sweep is not .js-only, in one line; the measurement date goes to the history document",
    `.ts AND .tsx TOO: a ?url import pointed at a .ts source emits the raw source, so a .js-only
sweep would grade every healthy asset and skip the defective one.`,
  ],
  "scripts/check-page-payload.mjs#29": ["WHY", "why the copy is .mjs; two lines already"],
  "scripts/check-page-payload.mjs#30": ["WHY", "why the Error line and not a tail slice; two lines already"],
  "scripts/check-page-payload.mjs#31": [
    "WHY",
    "why this floor is outside the floors mechanism; the dated 78-against-15 reading goes to the history document",
    `NOT an \`assertFloor\`: this counts BUILT CHUNKS rather than assertions executed, a SCOPE floor
in the sense of hard rule 10. The count is a property of the bundler's splitting on the day, so
\`check:floors\` would read the gap as drift.`,
  ],
  "scripts/check-page-payload.mjs#32": ["CONTRACT", "section marker, rule padding cut", `hydration is opt-in, and only the admin plane and its door opt in`],
  "scripts/check-page-payload.mjs#33": [
    "CONTRACT",
    "what the set is pinned to and where a public route fails first, in one line",
    `Pinned to exactly admin.tsx (which covers its children) and login.tsx, so a public route
gaining the flag fails HERE, by name, before check:browser has to notice.`,
  ],
  "scripts/check-page-payload.mjs#34": [
    "CONTRACT",
    "the window and the strip rule, in one line",
    `The window is the Layout return, bounded by the two literals; comments are stripped because a
gate reading source can be satisfied by a comment.`,
  ],
  "scripts/check-page-payload.mjs#35": ["CONTRACT", "section marker, rule padding cut", `no ineffective dynamic import`],
  "scripts/check-page-payload.mjs#36": [
    "WHY",
    "the boundary and the enumerated blind spot; the revisions-route account and the scenery argument go to the history document",
    `**This does NOT read the build log.** No gate here runs the build, so the criterion for a
dynamic import that splits nothing is derived from SOURCE: it is ineffective when some module
in the same graph also imports it statically. Bare package specifiers and virtual modules are
skipped, having no resolver. The one it judges is the CodeMirror split, which hard rule 4
names.`,
  ],
  "scripts/check-page-payload.mjs#37": [
    "CONTRACT",
    "why the finder is pure, in one line",
    `Pure over its input so the self-test below can run it on synthetic sources, the documented
cure for a per-item assertion whose real collection can shrink to nothing.`,
  ],
  "scripts/check-page-payload.mjs#39": [
    "WHY",
    "what the normalisation does not match, in one line; the alias-table argument goes out",
    `By NORMALISED SPECIFIER, not resolved path, so an alias spelling and a relative spelling of
one module do not match. Stated rather than hidden: one project alias is used consistently.`,
  ],
  "scripts/check-page-payload.mjs#42": ["CONTRACT", "trailing tag on the branch it labels; one line already"],
  "scripts/check-page-payload.mjs#43": ["CONTRACT", "type annotation plus what the map is keyed by; one line already"],
  "scripts/check-page-payload.mjs#46": ["WHY", "why a type position is not a dynamic import; two lines already"],
  "scripts/check-page-payload.mjs#48": [
    "WHY",
    "why the self-test runs every time, in one line",
    `SELF-TEST, on every execution: one synthetic file must be reported and one must not, so the
finder is proven able to fire on a day the tree contains no dynamic import.`,
  ],
  "scripts/check-page-payload.mjs#49": [
    "WHY",
    "why the path is repo-relative; the fixed-depth attempt and its plant go to the history document",
    `Relative to the repo root, so the failure names a path someone can open.`,
  ],
  "scripts/check-page-payload.mjs#50": [
    "WHY",
    "why workerd refuses it and why the weak two stay; the arrival date and the ownership argument go to the history document",
    `A NATIVE BUILD-TIME DEPENDENCY MAY NOT REACH THE WORKER: \`workerd\` has no filesystem and no
native modules, so an import that reached it is not a size problem, it is a Worker that fails
to start. THE THIRD ASSERTION IS THE POINT, because a source scan and a manifest say nothing
about what SHIPPED; the weak two stay because they name the defect at the right altitude.`,
  ],
  "scripts/check-page-payload.mjs#51": [
    "WHY",
    "the comment-satisfied-anchor trap, in one line",
    `Comments stripped: a comment naming the module has both satisfied and failed an assertion in
this repo before.`,
  ],
  "scripts/check-page-payload.mjs#52": ["WHY", "the zero-scope class with its citation; three lines already"],
  "scripts/check-page-payload.mjs#53": [
    "CONTRACT",
    "what the artifact is and why a bare substring, in one line",
    `\`build/server/index.js\` is what wrangler uploads. A bare substring, not an import pattern:
the bundler rewrites syntax and a require can survive as a string, a banner or an external.`,
  ],
  "scripts/check-page-payload.mjs#54": ["CONTRACT", "one line already; kept"],
  "scripts/check-page-payload.mjs#55": [
    "CONTRACT",
    "the subject, where the sets come from and the one thing it does not own; the resolve and refuse tables restate the code below",
    `THE WHOLE PAGE, PER ROUTE. Rule 4's subject is the stylesheets and the fonts as much as the
JavaScript the gate above grades. Resolved offline per \`lib/page-payload.mjs\`, by reachability
over the route's import graph, which over-approximates: the safe direction for a ceiling.

It does NOT own the speculation self-reference, which \`test/header-speculation.test.mjs\`
asserts; named here only so a reader does not conclude it is ungated.`,
  ],
  "scripts/check-page-payload.mjs#56": [
    "NUMBER",
    "why both palettes ship, how a ceiling was set and the expiry contract; the uplift bytes, the six token counts, ruling 103's account and the three self-policing rules the code asserts go to the history document",
    `THE REDESIGN UPLIFT, and it is TEMPORARY BY CONSTRUCTION.

Both palettes ship at once because the old one still has consumers, and deleting its block
would fail section 31's "every referenced token is defined" on all of them. Each ceiling kept
the headroom it had, so nothing here widens a margin, it moves the floor the margin sits on.

THE DATE IS THE WHOLE CONTRACT, and a build number is the wrong one: a deadline that assumes a
schedule which is not running is not a deadline. These come down by UPLIFT_EXPIRES or this
gate fails, and moving the date is a ruling. The same date governs section 31's carried map.`,
  ],
  "scripts/check-page-payload.mjs#57": [
    "NUMBER",
    "what the two figures mean, why fonts are separate and the both-directions rule; the dated measurement and the margin discipline go out",
    `PER-ROUTE CEILINGS, in BROTLI bytes. The only copies, rule 17.

\`css\` is the stylesheets, \`total\` adds the bundles. Fonts are excluded and asserted
separately, because the shared face counted into every total would say the site is eight fonts
heavy. A route missing here FAILS, both directions.

@type {Record<string, { id: string, css: number, total: number }>}`,
  ],
  "scripts/check-page-payload.mjs#59": ["CONTRACT", "what the extra key is; one line already"],
  "scripts/check-page-payload.mjs#60": [
    "NUMBER",
    "the one-bar argument, stated here because the entries below point at it; the measurement goes out",
    `\`/blog\`'s ceilings: the same kind of listing from the same stylesheets, so the two are graded
against one bar rather than drifting apart by whichever was measured later.`,
  ],
  "scripts/check-page-payload.mjs#61": [
    "NUMBER",
    "one line pointing at the argument above",
    `The tag archive's, for the reason above: one bar for one kind of page.`,
  ],
  "scripts/check-page-payload.mjs#62": [
    "NUMBER",
    "which pages share this bar, in one line",
    `/privacy and /colophon's shape, app.css plus prose.css and one bundle. One bar for one kind
of page.`,
  ],
  "scripts/check-page-payload.mjs#63": [
    "WHY",
    "why the largest corpus is the leanest page; the shared-bar restatement and the record counts go out",
    `\`/projects\`' ceilings, the same page shape. WHAT THIS ROUTE DOES NOT PAY FOR: only the loader
touches \`PUBLICATIONS\` and the public plane does not hydrate, so the records render to HTML
and are never serialised into a payload.`,
  ],
  "scripts/check-page-payload.mjs#64": [
    "WHY",
    "why one sheet serves both pages and what the ceiling does not measure; the measurement and the 144-author figure go out",
    `The index's ceiling, because it links the same two stylesheets and \`publications.css\` carries
both pages. The ceiling measures the SHARED cold load a browser caches once, so this page's
own HTML is the variable part: a long author list is content, not a payload regression.`,
  ],
  "scripts/check-page-payload.mjs#65": [
    "NUMBER",
    "why the margin is the base route's; the dated measurement goes out",
    `THE MATH VARIANT'S CEILINGS. \`/blog/:slug\`'s own margins proportionally, because this IS that
route with one more sheet, and that sheet is generated from a pinned package.`,
  ],
  "scripts/check-page-payload.mjs#66": [
    "NUMBER",
    "why a floor and not an equality, in one line",
    `A floor rather than an equality, so an upstream face ADDED in a later version does not fail
while the trim in \`build-katex.mjs\` dropping one does.`,
  ],
  "scripts/check-page-payload.mjs#67": [
    "WHY",
    "why an unused preload is worse than none; the face's byte size and the ownership pointer go out",
    `A preload that goes unused within a few seconds is worse than none: the browser warns and the
bytes compete with the ones that were needed. The per-face argument is root.tsx's.

@type {Record<string, string>}`,
  ],
  "scripts/check-page-payload.mjs#68": [
    "WHY",
    "why the one preload goes to Inter; the step citation and the CLS figure go to the history document",
    `THE SERIF IS THE LATE FACE ON PURPOSE. Inter owns the page's dominant metrics and gets the
one preload; a second would put a whole face on every route's critical path for a few heading
lines. Late discovery is paid for by \`font-display: swap\` and the metric-override fallback.`,
  ],
  "scripts/check-page-payload.mjs#69": [
    "WHY",
    "why the palette is false everywhere, in one line",
    `The palette is \`false\` everywhere on purpose: \`theme.ts\` fetches it on the first search
gesture, so a route that starts reaching it has put a search dialog back on a document.`,
  ],
  "scripts/check-page-payload.mjs#72": ["CONTRACT", "one line already; kept"],
  "scripts/check-page-payload.mjs#74": ["CONTRACT", "one line already; kept"],
  "scripts/check-page-payload.mjs#76": [
    "CONTRACT",
    "the derived rule and why it matches check:browser's, in one line",
    `THE ROUTE SET IS DERIVED, then reconciled BOTH directions. A route exporting the shared cache
headers is a public HTML route, the same rule \`check:browser\` uses, so the two cannot
disagree about what public means.`,
  ],
  "scripts/check-page-payload.mjs#77": ["WHY", "why a widening must expire; three lines already"],
  "scripts/check-page-payload.mjs#78": [
    "WHY",
    "why the math key is not a route; the account of the gate catching its own author goes to the history document",
    `A second grading of /blog/:slug rather than a route of its own, so its
ceiling is MATH_CEILING and not a ROUTE_CEILINGS key.`,
  ],
  "scripts/check-page-payload.mjs#80": ["CONTRACT", "why root's assets are found once; one line already"],
  "scripts/check-page-payload.mjs#82": ["CONTRACT", "section marker, rule padding cut", `a reachable font is preloaded, or exempt with a reason`],
  "scripts/check-page-payload.mjs#83": [
    "WHY",
    "the prohibition and how the name is read; the two spellings of the failed guess go to the history document",
    `DERIVED, NOT GUESSED: a gate that invents the name it looks for is testing its own spelling.
Find root's \`?url\` import ending in this font file, take the local name it bound, require that
name in a preload entry.`,
  ],
  "scripts/check-page-payload.mjs#84": [
    "WHY",
    "the intended arrangement, in one line",
    `Asserted rather than assumed: \`theme.ts\` fetches the palette on a gesture, so stating the
arrangement is what makes its return read as a regression rather than a puzzle.`,
  ],
  "scripts/check-page-payload.mjs#85": [
    "CONTRACT",
    "why the route model cannot see this page and which two assertions are a pair; the other two restate the code below",
    `THE ONE PAGE THIS GATE'S ROUTE MODEL CANNOT SEE: the math sheet is linked PER POST, by root
from the loader's \`hasMath\`, so it is in no route's manifest and would otherwise ride on a
page with no ceiling anywhere.

THE FIRST TWO ASSERTIONS ARE A PAIR: "on no route's manifest" passes on a build where the
sheet was deleted, so "reachable from root" is what makes the pair a measurement.

@param {any} manifest @param {Set<string>} rootAssets @param {string} rootSource
@param {string} clientDir @param {(p: string) => string} assetFile`,
  ],
  "scripts/check-page-payload.mjs#86": ["CONTRACT", "section marker, rule padding cut", `2. the sheet is reachable from root, so 1 is not vacuous`],
  "scripts/check-page-payload.mjs#88": [
    "WHY",
    "why a set and not a count, in one line",
    `A SET, because the two walks OVERLAP: a specifier reachable from both is found twice and the
raw count says two where there is one file.`,
  ],
  "scripts/check-page-payload.mjs#89": [
    "WHY",
    "why neither the name nor byte equality works, in one line",
    `BY CONTENT, NOT BY NAME: a Vite hash may contain a dash, and byte equality is unavailable
because Vite compiles the sheet on the way through. The anchor is a rule only it can contain.`,
  ],
  "scripts/check-page-payload.mjs#91": ["CONTRACT", "section marker, rule padding cut", `1. and it is on no route's manifest, which is the mathless cost`],
  "scripts/check-page-payload.mjs#92": [
    "WHY",
    "the three source facts, one clause each; the dated check:browser failure goes to the history document",
    `Three source facts no other instrument can see. The \`<link>\` is guarded by \`linksMath\`, and
dropping the guard fails the manifest assertion from a direction it cannot see. It carries NO
\`precedence\`, which would hoist it above the \`color-scheme\` meta, and that meta is
load-bearing because it arrives before the first stylesheet request. Root names TWO ROUTE IDS,
reconciled against the routes returning \`blogPostView(...)\`.`,
  ],
  "scripts/check-page-payload.mjs#93": [
    "WHY",
    "what the handle buys and why the set is derived, in one line",
    `A route rendering \`<PostEditor\` copies this document's stylesheets into its preview iframe,
so without the handle an author typing an expression sees it unstyled. Derived, because a route
added later would inherit that silently.`,
  ],
  "scripts/check-page-payload.mjs#94": [
    "WHY",
    "why the discriminator is the projection call, in one line",
    `A route renders a post exactly when its loader returns the shared projection, so
\`blogPostView(\` is the discriminator rather than a name pattern.`,
  ],
  "scripts/check-page-payload.mjs#95": [
    "WHY",
    "the string-on-both-sides blind spot; the mentions incident goes to the history document",
    `A STRING on both sides: root casts the loader data, so a rename in \`blog-view.ts\` leaves root
reading \`undefined\`, linking nothing, and rendering every equation unstyled with a green
typecheck.`,
  ],
  "scripts/check-page-payload.mjs#96": ["CONTRACT", "section marker, rule padding cut", `3. what a math post actually costs`],
  "scripts/check-page-payload.mjs#97": ["CONTRACT", "section marker, rule padding cut", `4. the faces, and the one that was inlined`],
  "scripts/check-page-payload.mjs#98": [
    "WHY",
    "the CSP refusal and the split failure; the face's size and the inline limit go to the history document",
    `NOT A PREFERENCE: \`font-src\` is \`'self'\` with no \`data:\` source, so a face Vite inlined as
base64 would be refused while the others fetched, giving big delimiters in a fallback serif on
some equations and not others. \`vite.config.ts\` refuses to inline any \`.woff2\`; this says so,
because a config edit is invisible until something reads the build.`,
  ],
  "scripts/check-page-payload.mjs#99": [
    "WHY",
    "why this is the deliberate opposite of the loop's rule; the kilobyte figures go to the history document",
    `THE OPPOSITE OF THE RULE ABOVE, and so stated rather than left an omission. The site's own
face is used by every page; a KaTeX face is used by an EXPRESSION, so preloading the set would
be speculative fetches for a page that needs a fraction of it.`,
  ],
};
