# Old decision log: what the citers still lean on

Report only. Worktree `C:\Users\email\dev\worktrees\ruling-consolidation` at ce1dea4. Old log = Capsid `dustinedwards` `decisions.md` (vol 6) and `decisions-vol-1.md` to `decisions-vol-13.md`; there is no vol-6 file.

## Scope and method

Citers grepped: `CLAUDE.md`, `.design-sync/conventions.md`, every `scripts/check-*.mjs`, `scripts/lib/**`, `.claude/skills/**`, `.claude/hooks/**`, `FAILURES.md`, `VERIFICATION.md`, `docs/RUNBOOK.md`; Capsid `core.md`.

- `NOTES.md` does NOT exist in the worktree.
- Capsid-side `capsid/conventions.md` was NOT readable: the token is scoped to `dustinedwards` only.
- `core.md` cites no old-log volume; it points only at vol 19, vol 20 and `retired-2026-09.md`.
- Only ONE citer names an old volume by path (`FAILURES.md:21`, vol 7). `CLAUDE.md:121` names vol 19 and 20 only. `scripts/lib/decisions-volumes.mjs:17` mentions "the unnumbered volume is long frozen" as a regex boundary, not a citation.
- Every other hit is an implicit citation: "Ruled <date>", "the ruling", "Dustin's verdict", or a named principle. Each was traced to its old-volume entry by date and content. Hits whose source is vol 14 or later (rulings 1 to 133), `chart-stack.md` or another Capsid doc are out of scope and listed at the end.
- Numbered rulings checked for carriage: vols 14, 15, 16, 17, 18, 19, 20 and `retired-2026-09.md`, read in full.

## Table

| citer file:line | cited volume and entry | binding or history | what the rule says in one sentence | carried by a numbered ruling or hard rule? |
| --- | --- | --- | --- | --- |
| `scripts/check-headers.mjs:472` ("Ruled 2026-08-17") | vol 6 (`decisions.md`), 2026-08-16 "my CSP option C was FALSIFIED" (choice A vs B) and 2026-08-16 "the CSP is ENFORCED" (commit 20c27d6) | BINDING (the gate fails if the header reverts to Report-Only, and gives this ruling as the reason) | The existing nonce plus strict-dynamic CSP is ENFORCED, not Report-Only, on both exits, and the ten-minute shared-cache nonce window is accepted. | PARTIAL. R11 (with R16 merged into it) carries "the ten minute shared-cache nonce lifetime is unchanged". No numbered ruling or hard rule says the CSP is enforced rather than Report-Only. Only the gate states that. |
| `scripts/check-headers.mjs:24` ("The ratified set, transcribed from the ruling") | vol 8, 2026-08-23 "security batch" (Permissions-Policy derived from use, HSTS without preload) on top of the header set landed 2026-08-06 (e7ede14, not itself entered in vol 2) | BINDING (the expected values are held as "ratified" and compared to the source) | HSTS max-age one year without preload; nosniff; strict-origin-when-cross-origin; DENY framing; a Permissions-Policy that denies only the nine measured-unused features and never clipboard-write; COOP same-origin-allow-popups; CORP cross-origin. | NO. No hard rule covers security headers and no numbered ruling restates the set. HSTS includeSubDomains is in `CUTOVER.md` step 3.10. |
| `scripts/check-headers.mjs:923` ("Transcribed from the ruling") | vol 8, 2026-08-23 security batch and its same-day "MY CORRECTION 1" (explicit no-store stands, because the old version relied on an unstated default) | BINDING | `/api/health` declares `Cache-Control: no-store` itself instead of inheriting the Worker's default. | YES, in substance: HR8 (the Worker stamps `private, no-store` on anything that declares nothing, and a route never opts out of refusal). The "declare it explicitly" detail lives only in the gate. |
| `scripts/check-browser.mjs:1730` ("It was prerender until 2026-08-28") | vol 10, 2026-08-28 "prefetch fix SHIPPED" (Grok's prerender-activation diagnosis) and 2026-08-28 "Dustin's verdict on the blink: much better" | BINDING (gate: THE ACTION IS PREFETCH) | The public speculation rule's action is `prefetch`, never `prerender`, because a prerender that has not painted gets activated as a blank frame. | PARTIAL. HR4 says prefetch is a JS-only extra that nothing may depend on. It does not say prerender is forbidden. |
| `scripts/check-browser.mjs:1745` ("reverted on Dustin's verdict ... Its reappearance is that decision being undone") | vol 10, 2026-08-27 "prerender coverage arc SHIPPED" (header immediate list deleted per the seat's eagerness ruling), then the 2026-08-28 verdict | BINDING | There is exactly one speculation rule, at `moderate` eagerness, with no `immediate` rule for header destinations, because an immediate rule cost 4 to 5 extra credentialed origin requests on every page load. | NO. |
| `scripts/check-browser.mjs:1291` ("the escaped-text ruling") | vol 11, 2026-08-30 blog roadmap triage ("Webmentions render as ESCAPED TEXT, never injected") | BINDING | Mention author and excerpt render as escaped text, and a source href is a validated http(s) anchor, never injected markup. | YES: R5 (vol 14, item H ruling 5) restates it word for word. |
| `scripts/check-invariants.mjs:1686` ("iterating the guarded delete IS the ruling this feature was built under") | vol 6, 2026-08-15 media v6 arc session 2 and "COMPLETE" entries ("Empty trash iterating the existing guarded delete with per-key reporting"; PLANT 2) | BINDING | Empty trash claims every key through the refcount guard `claimMediaKeyForDelete` and deletes an object only after the claim succeeds. It never gets a direct delete loop of its own. | NO. The destructive-path family (R19, R34, HR18) does not state it. |
| `scripts/check-admin-ui.mjs:3006` ("the ruling the Cmd+K badge waited a whole session for") | vol 6, 2026-08-15 "the media mockup is IMPLEMENTED COMPLETELY" ("the ruling that withheld it is now SATISFIED"). The earlier withholding entry is not quoted in vol 6. | BINDING | The admin page must not document a keyboard shortcut that no island implements. | NO. The nearest is vol 18's 2026-09-14 narrative on the search hint's honesty contract, which is unnumbered. R100 restores the hint but does not state this rule. |
| `scripts/check-logo.mjs:331` ("carries exactly the ruled sizes") | vol 5, 2026-08-12 asset-suite rulings ("The ICO regenerates ... AT THE SIZES THE CURRENT ICO CONTAINS, measured") and "Asset suite SHIPPED" (16/32/48) | BINDING | `favicon.ico` carries exactly the ruled sizes: 16, 32 and 48, PNG-encoded. | NO numbered ruling. The values are owned by `scripts/fixtures/icon-suite.json`, which is HR17-compliant, but no ruling records why. |
| `scripts/check-config.mjs:262` ("If that is wanted, it needs a ruling, not a config edit") | vol 5, 2026-08-12 "Session F resolved" (logs privacy finding) and 2026-08-14 "F.1 shipped" (`invocation_logs: false`, proven by control) | BINDING | Workers invocation logs stay off on the site and the watchdog, because they would persist cookies and `cf-connecting-ip` for 7 days with no redaction. | NO. R40 (vol 16) covers Sentry traces. Its entry says in passing "invocation logs are off by ruling" but does not rule it. |
| Hard rule 10's "fixture independence", cited as law at `scripts/check-urls.mjs:231`, `scripts/check-hook-matchers.mjs:181`, `scripts/check-headers.mjs:223`, `scripts/check-floors.mjs:9`, `scripts/check-features.mjs:1632`, `scripts/lib/uptimerobot.mjs:5`, `scripts/check-admin-ui.mjs:262, 303, 2486` | vol 5, 2026-08-12 "The icon gate shipped in check:logo; the FIXTURE-INDEPENDENCE PRINCIPLE, recorded as law" | BINDING (each site gives it as the reason its expected values are hand-written) | A gate's expected values must never be produced by the process the gate checks. | NO, AND IT IS MISATTRIBUTED. Four sites (`check-features.mjs:1632`, `check-floors.mjs:9`, `check-hook-matchers.mjs:181`, `check-urls.mjs:231`) credit it to "hard rule 10", but neither `CLAUDE.md` HR10 nor `VERIFICATION.md` contains the phrase or the principle (grep: zero hits in both). It exists only in the frozen vol 5. |
| `.claude/hooks/scoped-git-add.sh:11` ("CANONICAL COPY, ruled 2026-08-16 ... neither is negotiable without a new ruling") and `:26` ("REJECTED as of 2026-08-16") | Fails-closed half: vol 3, 2026-08-11 "Items 8, 10, 11 and 14 land" (scoped-git-add.sh fails CLOSED with the interpreter probe). Whole-string matching and the canonical-copy ruling of 2026-08-16: NOT in the dustinedwards log (vol 6 is silent). The hook names `capsid/conventions.md` as canon, which is a portfolio document and unreadable from this scope. | BINDING | The staging guard fails closed on every non-3/non-0 checker outcome and matches the whole command string. A narrowed command-position matcher is rejected. | NO in dustinedwards. CLAUDE.md's session section says "never `git add -A`" but not the two properties. The portfolio home may carry them; that could not be checked. |
| `FAILURES.md:21` (`capsid:dustinedwards/decisions-vol-7.md`) | vol 7, 2026-08-21 "BINDINGS HARVESTED BACK from the core.md cut" ("EXTRACT BINDINGS FIRST, THEN CUT") | HISTORY (provenance) | When cutting a document, extract the bindings filed inside status paragraphs first, then cut. | The lesson is restated in full in `FAILURES.md` itself, and ruling 116's method (`retired-2026-09.md`: every retired item named with where it went) applies it. The pointer is provenance only. |
| `scripts/check-features.mjs:430` ("That machinery left git on 2026-08-26") | vol 9, 2026-08-26 "REVERSAL ... committed artifact comes OUT" and "the artifact arc SHIPPED" | HISTORY (the date explains the vocabulary ban) | Git holds markdown only; D1 holds the only rendered copy; a save commits one file. | YES: HR18 (the repository is the source, D1 is derived) and HR17 (the gated feature-prose clause). |
| `scripts/check-invariants.mjs:2153` ("This section was LAST until 2026-08-07") | vol 2, 2026-08-07 "The hard rules reconcile to ONE list of fifteen, and the numbering is FROZEN" | HISTORY | The hard rules come first, and their numbering is append-only. | YES: CLAUDE.md's hard-rules preamble ("Numbering is APPEND-ONLY") and check:invariants section 15. |
| `VERIFICATION.md:534-535, 552` ("That window closed on 2026-08-17: the CSP has been ENFORCED since 20c27d6") | vol 6, 2026-08-16 CSP entries and 2026-08-18 editor-regression entry | HISTORY (a dated worked case) | (The same CSP enforcement as the first row, told as method history.) | See the first row. |
| `.claude/hooks/stop-typecheck.sh:48` (check:hooks "deleted on 2026-08-21"), `.claude/hooks/no-em-dash.sh:8, 32` (fails closed 2026-07-27; Bash added 2026-08-12), `scripts/check-hook-matchers.mjs:288` (2026-08-14), `.claude/hooks/no-direct-deploy.sh:22` (fixed 2026-09-04) | vols 1, 3, 4 and 13, by date | HISTORY | Dated provenance for hook behavior. Each hook's operative rule is stated in the hook itself, and no-em-dash.sh names `capsid/conventions.md` as canon. | Not applicable (not cited as a ruling). |

### Hits outside the old log (not counted)

- `scripts/check-charts.mjs:28, 30` ("the ruling probe", ">= 3 processes"): `chart-stack.md` (a Capsid concept doc), not a decisions volume.
- `scripts/check-stack.mjs:195` ("the ruling requires these entries", the not-adopted list): no old-volume entry found. The term appears only in `publish-pipeline.md` and archived session notes.
- `scripts/check-secrets.mjs:96`: the "ratified secret list" is `REQUIRED_SECRETS`, which is HR3 plus check:invariants 27.
- `scripts/check-mail.mjs:211` (DMARC p=reject), `scripts/lib/ask-converge.mjs:13` (120 s window), `scripts/check-all.mjs:54` (2026-09-14), `scripts/check-hook-scope.mjs:179` and `.claude/hooks/no-direct-deploy.sh:32, 240, 319, 343` (2026-09-05/06, R20): these fall in vol 14 or later.
- `scripts/check-config.mjs:514` (traces off): R40.
- `scripts/check-invariants.mjs:4148, 4241` and `scripts/check-page-payload.mjs:603, 822`: R103.
- `CLAUDE.md:117`: vol 18.
- `.design-sync/conventions.md:199`: R128.
- All other date strings in the citers (about 80) are "measured on" stamps or fixtures, not citations of a decision.

## Cited as binding and carried by no numbered ruling or hard rule (the seat must re-home these)

1. **CSP is ENFORCED, not Report-Only** (vol 6, 2026-08-16; cited `check-headers.mjs:472`). R11 carries only the ten-minute nonce window.
2. **The ratified security-header set** (vol 8, 2026-08-23, on the 2026-08-06 set; cited `check-headers.mjs:24`). This covers HSTS without preload and Permissions-Policy derived from use, never denying clipboard-write.
3. **Speculation action is prefetch, never prerender** (vol 10, 2026-08-28; cited `check-browser.mjs:1730`). HR4 covers only "nothing depends on prefetch".
4. **One speculation rule at moderate eagerness, no immediate header rule** (vol 10, 2026-08-27/28, Dustin's verdict; cited `check-browser.mjs:1745`).
5. **Empty trash iterates the refcount-guarded delete, claim before delete** (vol 6, 2026-08-15; cited `check-invariants.mjs:1686`).
6. **No documented shortcut without an implementing island** (vol 6, 2026-08-15, the Cmd+K badge; cited `check-admin-ui.mjs:3006`).
7. **The ICO carries exactly 16/32/48** (vol 5, 2026-08-12; cited `check-logo.mjs:331`). The values are gate-owned but the reason is not homed.
8. **Invocation logs stay off, on both Workers** (vol 5, 2026-08-12/14; cited `check-config.mjs:262`). R40 mentions it only in passing.
9. **Fixture independence** (vol 5, 2026-08-12, "recorded as law"; cited at 9 sites in 7 files). This is the most urgent one. Four sites credit it to hard rule 10, but neither HR10 nor `VERIFICATION.md` states it, so the citations point at a text that does not contain the rule.
10. **scoped-git-add.sh's two canonical properties, fail closed and whole-string match** (fails closed: vol 3, 2026-08-11; the 2026-08-16 canonical-copy ruling is not in the dustinedwards log; cited `scoped-git-add.sh:11, 26`). It may be homed in `capsid/conventions.md`, which could not be read from this scope.

Binding and carried: the `/api/health` explicit no-store (HR8) and escaped-text mentions (R5).

**Counts:** 12 binding citation rows. Fixture independence is counted once, but it covers 9 citing lines in 7 files, so there are 20 binding citing lines in all. 10 of the 12 rows are un-homed, 2 of them partially carried (CSP by R11, prefetch by HR4). 2 are carried (HR8, R5). 5 rows are history only.
