// Ruling 115 decisions, blocks 0 to 235. id -> [tag, reason, replacement].
// replacement: a string replaces the prose (lines joined by \n), null deletes
// the block, and an absent third entry leaves it byte-identical.
const SPLIT = "Import order is the cascade: moving an import, or a rule between files, can change what renders.";
export default {
  0: ["WHY", "admin bundle and cascade whys kept; split date and bundle sizes are history", "The admin plane's sheets, imported only by routes/admin.tsx and routes/login.tsx,\nso a public reader never downloads them. Import order is the cascade; do not reorder.\nNo reset here: app.css loads it on every route, and a second import would duplicate it."],
  1: ["WHY","CSP and cache whys kept; self-hosting history and weight census moved; restored after Grok review 89352d6","Inter, self-hosted so style-src and font-src stay 'self': variable latin subsets, weight axis 100 to 900.\nBuilt through Vite, not public/, so the hashed URL is cached immutable; never mark stable /fonts/ paths\nimmutable. unicode-range keeps the italic to pages with italic latin text. font-display differs per face:\nsee the next note."],
  2: ["NUMBER", "CLS figures are a dated measurement with no gate; the why stays", "font-display: optional on the normal face, because swap re-wrapped the /blog chip row and one\nsize-adjust cannot match every weight. The italic keeps swap and is not preloaded: it is needed\nonly with italic text, and optional on a late face means it is almost never used."],
  3: ["HISTORY", "ruling date and the declined subsetting; its why is carried by the two blocks above", null],
  4: ["NUMBER","metric table is a dated measurement; the declarations own the shipped values; restored after Grok review 89352d6","Arial sized to Inter, tuned at 17px, the prose size. Prose computes font-optical-sizing: auto, so opsz\nmoves with the font size and Inter's advance width varies (ascent and descent do not). Never retune from\nthe usual 1000px sample: that ratio makes body text narrower than plain Arial. Only Arial, which Helvetica\nand sans-serif resolved to when measured; other local() faces would inherit its numbers."],
  5: ["WHY", "serif scope, namespacing and swap whys kept", "The serif sets public headings at 32px and up, nothing else. Namespaced \"Web\" so an installed retail\ncopy with other metrics is never used; check:fonts binds the declaration to the file. swap, not\noptional: this late face is not preloaded, and the fallback below keeps the swap from moving a line."],
  6: ["NUMBER", "metric table and CLS arms are a dated measurement; the Georgia-bold trap stays", "Georgia sized to the serif, tuned to the 2rem h2. Measure against Georgia REGULAR: with\nfont-weight: 200 900 the browser never synthesises bold, so a ratio taken against the bold is wrong.\nOnly Georgia: one face carries one set of overrides, and no second face is guessed."],
  7: ["WHY", "the at-rule trap kept; Tailwind history moved", "A plain :root, not @theme: an unknown at-rule is dropped whole, and every font fell back to Times."],
  8: ["WHY", "one-owner and order whys kept; the old-to-new remap table is history", "The stacking scale: one named token per layer. z-index compares by order, so the gaps only leave room.\nLow to high: tile raise, body, action; sticky; dropdown; popover; drawer scrim, drawer; pinned bulk bar;\noverlay scrim, overlay; progress; transient; skip link last. Not a colour: check:contrast needs hexes."],
  23: ["WHY", "ruling 96 z-index trap kept; the handoff's rejected numbering is history", "The header's layers: bar --z-pinned-bar, overlay menu --z-overlay, skip link --z-skip-link.\nNever --z-dropdown for the menu: it sits below the bar, which the menu must clear. The name is the trap."],
  27: ["WHY", "one owner and gate reason kept; 'agreed by chance' is history", "The elevation shadow, one owner. Not in the colour blocks: check:contrast needs a hex in a measured pair."],
  28: ["WHY","the shorthand ban and the weight and optical-size whys kept (Grok's restored points); restored after Grok review 89352d6","Type levels: a consumer sets all five properties, never the font shorthand, which drops the variation axes.\nWeight is set twice to one number: font-weight selects the face, wght positions the axis. With only wght a\nlevel matches the 400 face and draws it heavier; with only font-weight the browser interpolates the axis.\nOptical sizing is off, so nothing interpolates on resize."],
  37: ["WHY","reader list and no-reader refusal kept; step 2 and step 6b history moved","The strong nav weight, a font-weight number: active bar destination, current filter chip, caption lead-in.\nNo 640 level and no opsz companion: a level token with no reader is refused, so it is not carried."],
  39: ["WHY", "one-breakpoint rule kept", "The only breakpoint in the type scale: at 48rem the two serif levels step to optical size 48.\nInter levels never appear here, because Inter's opsz axis stops at 32."],
  40: ["WHY", "gate reason kept; handoff history moved", "Space and dimension scales, declared once in a bare :root no theme overrides:\ncheck:contrast requires every token in the theme blocks to be a hex in a measured pair."],
  43: ["WHY", "hit target and AA floor distinction kept", "Control dimensions are not spacing and sit off the scale. 44px is the header hit target (WCAG 2.5.5,\nAAA), deliberate for one-handed reading. Never read it as the AA floor: 2.5.8 asks 24px, --control-min-dense."],
  45: ["WHY", "naming and easing rules kept", "Motion: four durations named by job, never by speed or number, which invites reuse by resemblance.\nA case that fits none of them usually should not animate. Easings end flat: no overshoot or spring."],
  48: ["WHY", "fade window and the not-the-reveal rule kept", "The overlay menu fade: under about 220ms a fade flickers, over about 300ms the reader waits.\nNot the scroll reveal: a view() timeline ignores duration, so this is --motion-panel, not --motion-reveal."],
  50: ["WHY","copy-not-derive, role naming, resolution order and parity kept; ratification date moved","Hill Country tokens: hexes copied from dustinedwards/design-tokens.md, never derived; named by role, never hue.\nLight is :root and [data-theme=\"light\"]; dark is :root:not([data-theme]) under a dark OS preference, and\n[data-theme=\"dark\"]. No attribute means system, resolved in CSS, so no blocking script. The blocks do not\ncascade into each other, so both must declare the same names; check:contrast recomputes and asserts both."],
  52: ["WHY", "exemption and cue rules kept; ratios are check:contrast's; the deleted token is history", "Deliberately below the contrast floor: WCAG 1.4.3 and 1.4.11 exempt inactive controls, and 4.5:1 would\nread as available. The disabled attribute, the recess and text saying why identify it; check:contrast\ncarries the exemption. cursor: not-allowed is not a cue: pointer-only, and it arrives too late."],
  54: ["WHY","chrome measuring rule and logo binding kept; ratios are check:contrast's; provisional review history moved","Chrome role tokens: the header and footer are a brand surface, so text on them is measured against the\nchrome. --mark-on-chrome is the dark brand hex in both themes, by ruling (the light logo reads on purple).\ncheck:logo binds it; change both in one commit, never silently."],
  55: ["WHY","opaque-scrim reason kept; glass floor figures moved","The caption scrim is opaque on purpose: fading to transparent makes the photograph the backdrop, so\ncontrast would vary by image and cross the 4.5:1 text floor. Same value in both themes: a scrim is a hole\nin the page. check:contrast carries the white-on-scrim pair."],
  56: ["WHY", "decorative-only rule kept; deleted variants are history", "Accent: terracotta. Decorative only; it never signals a state.\nOne token: variants would need contrast pairs of their own, and nothing paints them."],
  57: ["WHY", "reversible-versus-irreversible distinction kept; mockup ratios are check:contrast's", "Destructive: the REVERSIBLE removal (Trash, Empty trash). --*-danger marks the irreversible act\nand stays rare; danger red here would teach that trash is as final as delete.\nA peer of --text-danger, not a weaker sibling."],
  60: ["WHY", "role-name reason kept; the build-number state claim removed", "PAPER, GLASS, LIGHT (ruling 65): the redesign's role names.\nThe Hill Country roles above stay until their consumers migrate."],
  81: ["WHY", "one-token reason kept; bar removal and ruling 6 history moved", "The lamp catch hue follows the backdrop, not the theme: one backdrop, one token.\nA mention in check-contrast.mjs is not a read (ruling 104), so no second token waits for a bar."],
  86: ["WHY", "no-new-surface and ring rule kept; ratios are check:contrast's", "Dark chrome is the ratified hero surface, so no new surface enters the palette.\nThe ring is --focus-ring-on-chrome, never --focus-ring-on-brand, which is invisible on deep purple."],
  88: ["WHY", "mirror reason kept; ratio figures are check:contrast's", "The rust, lifted for dark to mirror the light ratios rather than to look similar,\nso the control reads with the same weight in both modes."],
  140: ["WHY", "placement, gate-order and @property whys kept", "Lamp geometry: a position and two numbers, so not in the palette blocks, whose tokens must be hexes.\nMust sit after them: check:contrast takes the first matching selector and asserts each carries --paper.\n--surface-catch is registered with @property so it interpolates; unregistered, the lamp would flicker."],
  142: ["WHY", "trimmed", "Dark sits further in and is stronger: at the edge the catch is lost to the bar's own value,\nand dark composites sit on a steeper part of the transfer curve."],
  144: ["WHY", "narrow-tier and literal-hex rules kept", "prefers-contrast: more is deliberately narrow: two tokens move and the border promotes to its strong\ntwin; nothing else changes. Literal hexes, not var(): check:contrast asserts every token is a hex."],
  145: ["WHY", "trimmed", "forced-colors: the palette yields to system colours and nothing re-asserts a brand colour.\nA transparent outline keeps each control's edge once its fill is gone; focus is already an outline."],
  146: ["WHY", "trimmed", "The admin topbar and sidebar edges are box-shadows, since a border would shift aligned heights, and\nforced-colors strips shadows. Real borders come back here: visible structure beats a sub-pixel."],
  147: ["WHY", "promotion date moved", "The admin chip's active state is a brand fill, which this mode removes,\nso it takes the same forced outline as the search chip."],
  151: ["WHY", "trimmed", "The word is the pill's first channel; border-style is the second and survives this mode, so the pills\ndiffer by stroke, not only hue. Restated on the outline, since the forced palette may flatten the border."],
  154: ["WHY", "placement whys kept; amendment date and old value moved", "--site-inset: the x of the identity mark on both planes and in both sidebar states. On body, not the\ntoken block (check:contrast parses those as colours), and not .admin, because the public chrome reads it."],
  155: ["WHY", "CVD grounds, exemption and specificity kept; decision date moved", "Rule 2: links are underlined, because brand and visited purples fail CVD simulation on hue alone.\nOnly an element with its own border or fill (chips, buttons) opts out, at its own rule.\nElement-level selectors, so any component rule that sets a colour outranks them."],
  159: ["WHY", "placement trade and one-card-owner kept; byte sizes moved", "The front door lives here, not in a route sheet: it styles the page every reader lands on, and a split\nwould add a request before first paint. Post cards reuse blog-index.css; a second style is two owners."],
  163: ["WHY","outline rule, inset ring and danger exclusion kept; ring ratios are measurements","Focus is an outline, never a box-shadow: forced-colors (Windows High Contrast) strips shadows. Filled\ncontrols draw it inside with a negative offset, in the ring measured against the brand fill. Never on the\ndanger fill, where that ring is invisible: delete keeps only the outer ring on the page background."],
  164: ["WHY", "trimmed", "The site mark: the five purple paths take --brand, resolved per theme by the token selectors, so the\nmark needs no media query and never flashes. The three warm paths keep the ratified literal fills."],
  165: ["WHY", "cascade and token-path whys kept; split history moved", "Below this point the sheets are imported by root.tsx, in order: import order is the cascade.\nThe tokens stay in this file because scripts/lib/tokens.mjs and check:contrast parse this path."],
  166: ["WHY", "import mechanism why kept; split measurements and postcss proof moved", "Public sheets only; the admin sheets load through app/admin.css. They are JavaScript imports in\nroot.tsx, in cut order, after this file's rules: CSS drops an @import that follows any rule,\nand hoisting them to the top would move the cascade."],
  167: ["WHY", "cascade rule kept; split history moved", "The editor's feedback slot and upload row.\n" + SPLIT],
  168: ["WHY", "trimmed", "The feedback slot: one node, four states, so two semantic tints never appear together (rule 4).\nThe empty slot stays rendered because it is the live region; a container that appears with its text\nannounces nothing."],
  172: ["WHY", "trimmed", "The leave guard takes the warning family: nothing is lost and the crash net holds the text, and it\nadds no contrast pair. Above the form, not a modal, which would take focus from the writing."],
  174: ["WHY", "cascade rule kept; split history moved", "The three-region post editor: bar, panes, CodeMirror, settings drawer.\n" + SPLIT],
  175: ["WHY", "trimmed", "The three-region editor: a fixed command bar, a canvas that is the ONLY scroll context, and a drawer\nnot laid out until opened. A second scroll context is a scrollbar nobody finds."],
  177: ["WHY", "trimmed; the 1280px wrap is history", "A three-column grid, not space-between, so the centre group stays centred whatever the sides' widths.\nFixed height (--admin-cmdbar-h), because the canvas subtracts it. Nothing in the bar may wrap."],
  178: ["WHY", "scrollport rule kept; the dated elementFromPoint finding moved", "Sticky offsets are measured from the scrollport, and .admin-content already starts below the topbar,\nso a non-zero top pushes the bar over the title input. Only the narrow breakpoint, where the document\nscrolls, sets a real offset."],
  179: ["WHY", "trimmed", "One control height for the whole bar; every control opts in here so the row cannot go ragged.\n2rem clears the 24px WCAG 2.2 target minimum."],
  182: ["WHY", "trimmed", "The buffer age (ruling 3) is quieter than the dirty indicator: muted, not warning, since two\nwarning-coloured items read as two problems. It reuses a pair check:contrast already measures."],
  183: ["WHY", "declared-height reason kept; uploader anecdote moved", "Region 2, the only scroll context. The height is declared (viewport less both bars), not left to flex,\nso nothing added to the column can silently shrink it. Nothing may render below this element."],
  187: ["WHY", "trimmed", "The title tracks the editor pane, not the page, using the grid's gap so the two cannot drift.\nPreview view has one column and needs no rule."],
  188: ["WHY", "trimmed", "field-sizing: content puts the counter right after the title text. Without it the input keeps\nflex: 1 and the counter sits at the row's right edge, already narrowed to the editor pane."],
  195: ["WHY", "prohibition kept; the 20rem history moved", "No min-height: a minimum taller than the canvas gave the preview two stacked scrollbars.\nThe pane fills what it is given, and the iframe scrolls the rest."],
  212: ["WHY", "dialog and border rules kept; session history moved", "Region 3, the settings drawer: a real <dialog> with showModal(), so focus trap, Escape and focus\nreturn are the platform's. It sits on the popover step, so every border inside is --border-strong:\n--border fails 3:1 there in dark."],
  214: ["WHY", "trimmed", "Frontmatter controls are a <details> in the form, not a drawer section, so they work without script.\nIts edge is --border-strong (rule 5): it is an elevated surface above the canvas."],
  215: ["WHY", "cascade rule kept; split history moved", "Admin version history.\n" + SPLIT],
  217: ["WHY", "trimmed", "The documented exception to rule 4: a diff must tell two states apart, so both tints are load-bearing.\nRule 1 still holds: each line keeps its literal + or - prefix."],
  218: ["WHY","reorder reason kept; split history moved","More media library, separate from admin-media.css because /playground sat between them in the original\norder. Import order is the cascade: merging the two, moving an import, or moving a rule between files can\nchange what renders."],
  219: ["WHY", "placement why kept; the move and its winner measurement moved", "Admin-only rules that must load after admin-media.css: in the public bundle they would stop\noverriding it (.media-col-usage). Their position here preserves the cascade exactly."],
  220: ["WHY", "token mapping and rule 1 kept; mockup literals moved", "Usage states take tokens, not mockup literals: used is --fill-success; in template is --brand (it\nbelongs to the site, not a semantic state, so rule 6 holds); unattached is --text-accent, always beside\nthe word. Every dot is aria-hidden with its sentence in text: colour is never the only channel (rule 1)."],
  222: ["WHY", "trimmed", "The usage cell is a column: as a flex row, the flags became a second item and clipped the state."],
  223: ["WHY", "trimmed", "Per-row flags take --text-accent, not a literal. Quiet by size: a flag is a note, not an alarm."],
  225: ["WHY", "trimmed", "Grid only: in the list a dot would land on the thumbnail, and the Usage cell already says it in words."],
  228: ["WHY", "undeclared-token trap kept", "The lens note keeps a narrowed view from reading as an accusation. --border-warning, since there is no\n--border-accent: an undeclared token resolves to nothing and check:contrast cannot see it."],
  229: ["WHY", "trimmed", "Three empty states. Only library-empty gets a heading, paragraph and button: it is the only one where\nthe reader must learn what the page is for."],
  231: ["WHY", "trimmed", "A dashed outline means offered, solid means applied: the suggestion and tag chips share size and row,\nso shape separates them without colour."],
  235: ["WHY", "trimmed; mockup literals moved", "Palette, shortcuts panel, toast and keyboard marks: all need script and degrade to nothing.\nMockup literals resolve to tokens; the toast uses --scrim and --on-scrim, which check:contrast measures."],
};
