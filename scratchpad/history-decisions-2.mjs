// Ruling 115 decisions, blocks 236 to 357. Same shape as history-decisions-1.mjs.
const SPLIT = "Import order is the cascade: moving an import, or a rule between files, can change what renders.";
export default {
  242: ["WHY", "pointer-events and token whys kept; mockup literals and ratio moved", "The toast: bottom centre, over everything, and it never takes pointer events, so it cannot swallow\na click meant for the grid. --scrim and --on-scrim, a pair check:contrast already measures."],
  246: ["WHY", "refusal kept; session label moved", "Page head, floating selection bar and type. The mockup's five failing colours stay refused:\nseparation is fill, and quietness is size, weight and tracking."],
  247: ["HISTORY", "y positions and removed rows: a dated measurement of a fixed layout", null],
  249: ["NUMBER", "gap measurements vs mockup; no gate; the scoping why stays", "The rows above the grid close up; stacked panel gaps were costing the grid height.\nScoped to the media panel so no other admin section moves."],
  251: ["WHY", "selection-is-a-mode why kept; old position and ratio moved", "The floating selection bar: a selection is a MODE that persists while you scroll, so its controls\npersist too. --scrim and --on-scrim, a pair check:contrast already measures."],
  252: ["NUMBER", "measured box sizes at 56 and 64rem; no gate; the prohibition stays", "64rem, not 56: this bar also carries the bulk tagging controls, and narrower it wraps into a block over\nthe grid. Two rows at 1280 is the honest residue; do not hide a control to fix it."],
  255: ["WHY", "families-are-Dustin's rule kept; the false first draft and CSP narrative moved", "Type: the families are not changed here. --font-sans is the site's brand face and --font-mono a\nfree system stack; changing either is Dustin's call. Only size, weight and tracking follow the mockup."],
  256: ["HISTORY", "old values of a replaced rule", null],
  257: ["WHY", "old value moved", "The group heading is mono: it is a machine's label for a folder, not prose.\nSmall, medium weight, wide tracking, uppercase."],
  258: ["NUMBER", "row height arithmetic against the mockup; no gate", "The list row: line height and padding match the mockup, because a taller row shows fewer files."],
  259: ["WHY", "trimmed", "The copy cell is a flex box: as a line box, the inline-block button gains a strut descent\nthat pulls the glyph off the row's optical centre."],
  260: ["WHY", "trimmed", "The confirmation modal replaces window.prompt, which is unstyleable, mute to a screen reader and\nsilently disabled in some contexts. Centred, not docked: a confirmation stops everything."],
  266: ["WHY", "cascade rule kept; split history moved", "The media library: grid, facets, SERP and social previews, document card, caption bar, tile, list.\n" + SPLIT],
  267: ["WHY", "trimmed", "Media library. The grid sits on the canvas, so --border; the picker is on the popover step, so\n--border-strong. Tiles render a transform with matching width and height, so the grid never reflows."],
  268: ["NUMBER", "column table from one rendering; no gate asserts it and check:admin-ui cannot see styling", "Density is measured in a browser, not calculated: the grid is narrower than .admin-content by its\npadding and the scrollbar. No gate asserts the column counts; re-render before trusting a number."],
  269: ["NUMBER", "the 1629ms median is a dated measurement; the behaviour whys stay", "Pending while a data-changing navigation is in flight (the router sets data-pending): an unmarked page\nreads as frozen and gets pressed twice. Rows dim and stop taking pointer input but never blank or\nreflow; aria-busy carries it. Never on a display change, which resolves without the loader."],
  271: ["WHY", "min() correctness and the tile-size prohibition kept; column and filename counts moved", "min(100%, ...) lets a viewport narrower than one tile yield one column instead of overflowing.\nThe mockup's 124px tile is not adopted: it brings back end-truncated filenames."],
  273: ["WHY", "trimmed", "The fixed box: an explicit ratio on the wrapper reserves the space before the image arrives,\nso a lazy tile cannot reflow the rows below and mixed ratios stay aligned."],
  274: ["WHY", "load-bearing rule kept; the shipped-defect measurements moved", "display: block is load-bearing: this is a <span>, and aspect-ratio does not apply to an inline box,\nso without it the fixed 3:2 box stops existing."],
  275: ["WHY", "floor why and undeclared-token trap kept; row counts moved", "The floor every tile paints on: SVGs get no LQIP, since the Images binding does not rasterize vectors.\nNever --surface-2: nothing declares it, so the declaration was invalid and the floor transparent,\nand check:contrast cannot see an undeclared token."],
  276: ["WHY", "trimmed", "The inline LQIP: cover and center mirror the object-fit below, so the swap is invisible.\nA tiny image scaled up is its own blur; no filter is needed."],
  278: ["WHY", "the removed squash is history; the 3:2 reason stays", "3:2 for documents too: DocumentCard fills the space with a title, text and size, so squashing it\nwould crush what fixed the wall of empty boxes."],
  279: ["WHY", "row counts moved", "What a document shows instead of an image: the Images binding cannot render a PDF, so it gets a\nlegible label rather than an empty box that reads as a failed load."],
  280: ["WHY", "trimmed", "The facet row: a fixed-width mono label, the chips, and a hint naming who owns the axis. The hint is\nthe design's argument, so it is copy in the markup. The fixed label width makes it read as label and set."],
  281: ["WHY", "the mockup ratio is a measurement; the text floor rule stays", "\"assigned by the system\": mono and muted, an annotation rather than a control. --text-muted, never a\nlighter grey: this is text a reader must read, so it meets the 4.5:1 text floor."],
  282: ["WHY", "trimmed", "The usage honesty line is a tinted box, not a muted sentence: it is what stops a reader deleting a file\nthe site serves. It sits on the accent tint, the palette's \"read this before acting\" surface."],
  283: ["WHY", "load-bearing rule kept; measured widths moved", "Name and Copy share a row. min-width: 0 is load-bearing: without it the grid item refuses to shrink\nbelow the name, and the tile's overflow clips the copy button while it stays focusable."],
  284: ["WHY", "trimmed", "The filename links to the detail view: the no-script route to the address Copy writes. No colour and no\nresting underline, which rule 2 allows because colour carries nothing alone; it underlines on hover and focus."],
  285: ["WHY", "trimmed", "One line: the string arrives middle-elided by middleTruncate. A line clamp was rejected because it\ncuts the end, the half that differs. The ellipsis backstops names wide in pixels."],
  287: ["WHY", "the containing-block fix kept; scroll-height measurements moved", "position: relative is the fix for the second scrollbar: the .sr-only label span is absolutely\npositioned, and without a positioned ancestor it escapes .admin-content's clip and stretches the\ndocument. data-copied is written by the click handler; this page has no component state, by ruling."],
  288: ["WHY", "the reset reason kept; the pixel accounting moved", "margin-top: 0 resets .btn-ghost's stacked-button margin, which is wrong for a glyph in a table cell\nand made the copy control the one child the row could not shrink."],
  289: ["WHY", "square-versus-label rule kept; overflow measurements moved", "A copy button with a visible word is not a 22px square: the square is sized for a tile, and the\ninspector's labelled snippet buttons overflowed it. :has() because the condition is \"contains a label\"."],
  290: ["WHY", "trimmed", "It wraps rather than clipping: nowrap with an ellipsis cut the usage word, the one word on this line\na reader is looking for."],
  291: ["WHY", "trimmed", "The detail view sits above the grid, not beside it: a side panel would take a third of the grid for\none asset. It is a ?key= on the same URL, so it is linkable and needs no script."],
  292: ["WHY", "fixed-drawer and dvh whys kept; the failed in-flow panel's measurements moved", "The inspector is a fixed drawer at the viewport's right edge over a scrim, and the grid behind it must\nnot move: inspecting is a side activity. Fixed escapes .admin-content's scroller, so no breakpoint is\nneeded. 100dvh, not 100vh, which is taller than the visible area on mobile Safari."],
  294: ["WHY", "trimmed", "The sticky head keeps the name and Close in view. Negative margins pull the band to the drawer's\npadding edge so it is full bleed."],
  295: ["WHY", "trimmed", "The name ellipsises rather than wrapping, which would push the panel down. min-width: 0 lets the flex\nitem shrink so Close stays on the panel; the full string is in the title and the Address field."],
  297: ["WHY", "trimmed", "The scrim is a real link, so closing works with no script and the whole outside area is the target.\nNot aria-hidden, which is wrong on a focusable control; it carries a real accessible name."],
  298: ["WHY", "the three refusal grounds kept briefly; the first attempt and mockup value moved", "Dimmed with opacity on the existing --scrim, never a new rgba() token: the token block takes literal\nhexes only, minification would change the value, and a veil with no text on it has no pair to measure."],
  299: ["WHY", "trimmed", "One column inside the side panel at every width: the two-column body pushed the facts column past\nthe panel edge. The panel is the narrow context, so it gets the narrow layout."],
  300: ["WHY", "trimmed", "Nothing in the panel may be wider than the panel. An <input> keeps an intrinsic width and does not\nshrink to its track: min-width: 0 on the row and width: 100% on the field are both needed."],
  301: ["WHY", "the old token name is history", "The same floor the tiles paint on, so a transparent PNG and an SVG with no LQIP read as they do in\nthe grid."],
  306: ["WHY", "border law and no-imitation rule kept", "SERP and social previews live in the drawer, on the popover step, so every border is --border-strong.\nNeither imitates a real engine's chrome: they show the strings the site emits, cut where they will be\ncut, because a pixel-faithful mock goes quietly wrong."],
  311: ["WHY", "trimmed", "The upload control: the file input is folded into its label so the head shows one button. Never\ndisplay: none, which is unreachable by keyboard; it is clipped sr-only, so focus lands on the label."],
  312: ["WHY", "mockup ratios moved; the fill-not-hairline rule stays", "The section heading is the spine: small caps, a count, a quiet right-aligned note. The mockup's failing\ngreys do not ship; quietness is size, weight and tracking, and separation is fill."],
  314: ["WHY", "trimmed", "The note: right aligned. --text-muted, the quietest token that still clears the 4.5:1 text floor."],
  316: ["WHY", "session label moved", "Group headings, selection and the drop hint. No hairline that fails a floor:\nthe heading separates by space and weight."],
  320: ["WHY", "the rebuilt-softness rule kept; the mockup's failing ratios moved", "The display bar, the two layouts and the destructive accent. The mockup's hairlines and greys fail\ntheir floors and do not ship, and no nearest compliant hairline replaces them: calm comes from tint,\nspacing and weight."],
  325: ["WHY", "trimmed; ratios moved", "Document card, caption bar and table. The mockup's failing colours stay refused. Where a line is the only\nhonest boundary it is --border, once for the table header, never once per row."],
  326: ["WHY", "the pair ratios are check:contrast's; row counts moved", "The document card: a label, a title and a foot, each a pair check:contrast already carries. The mockup's\npale label does not ship; size, tracking and uppercase make it read as a label."],
  327: ["WHY", "trimmed", "Three lines, then the ellipsis: a title is words, so a line clamp is right here and the cut lands\nat a line end. Filenames elsewhere are never clamped."],
  328: ["WHY", "trimmed", "The suggestion of text is aria-hidden decoration, so WCAG 1.4.11 does not reach it; it still takes\n--border, because this sheet never carries a colour nobody can see."],
  329: ["WHY", "trimmed", "More air than the mockup: at --border the rules are legible lines, and three of them tightly stacked\nread as a triple underline."],
  330: ["WHY", "trimmed", "Ragged, like text: a full-width rule reads as a divider, so all three stop short at different places.\nQuietness from shape, which costs no contrast."],
  331: ["WHY", "trimmed", "No card foot: the mockup's page count is fixture data this site cannot store. DocumentCard has the reasoning."],
  332: ["WHY", "trimmed", "Each tile size gives the card different room, so parts that need room appear only when it exists.\nAt S the rules go; nothing hidden carries meaning."],
  333: ["WHY", "trimmed", "The caption bar is a SIBLING of the thumbnail link, not a child: it carries a button, and a <button>\ninside an <a> is invalid markup browsers resolve inconsistently."],
  334: ["NUMBER", "measured bar heights; no gate; the why stays", "Tightened padding and explicit line-heights: a caption that covers the picture it labels is not a label."],
  339: ["WHY", "trimmed", "An outline, not a fill: a fill would be translucent white over the scrim, the unmeasurable composite the\nscrim exists to remove, and color-mix() here would confuse check:contrast."],
  340: ["WHY", "trimmed", "The tile is an explicitly placed grid: source order serves the list's eight columns while the tile reads\nthumbnail, then name and copy, then meta."],
  341: ["WHY", "one-tree and reveal rules kept; the before-state narrative moved", "The tile is the card: 3:2, edge to edge, with metadata over the image on hover or selection.\nNothing is removed from the markup, so the one-tree rule holds: the name link and copy button stay\nfocusable in source order and are revealed by hover, focus or selection."],
  343: ["WHY", "trimmed", "The body is the caption, laid over the image and revealed on hover, focus-within, selection and open.\nFocus is in the list because a keyboard reader never hovers."],
  348: ["WHY", "trimmed", "The hover lift: a shadow replaces the resting inset hairline, and the small transform makes it read as\na lift rather than a glow."],
  349: ["WHY", "no-token rule kept; the arc tally moved", "A literal rgb() shadow, not a --shadow-tile token: a shadow colour has no contrast pair, and a token\nwith none can never participate in check:contrast's matrix."],
  352: ["NUMBER", "checkbox geometry is in the declaration; the why stays", "The extension label clears the checkbox, which is absolutely positioned in the same corner.\nGrid only; in the list they are in different columns."],
  353: ["WHY", "trimmed", "A document tile hides its filename: the card already shows the title and extension. Hidden, not removed:\nthe anchor stays for the evaporation scan and the no-script route. Image tiles keep their name."],
  354: ["WHY", "trimmed", "With the name row hidden, the copy control drops onto the meta line, which gives up its second column."],
  355: ["WHY", "the separate-grids reason kept; track values are in the declaration", "The list is a real table: the header and each row are separate grids on an identical track list, so\ncolumns align while folder headings still break the rows into groups."],
  356: ["WHY", "the mockup ratio moved", "The header separates by fill plus one --border rule beneath it; rows separate by alternating fill,\nnever a rule per row."],
};
