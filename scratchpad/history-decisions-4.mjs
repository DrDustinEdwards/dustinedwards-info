// Ruling 115 decisions, blocks 488 to 615. Same shape as history-decisions-1.mjs.
const SPLIT = "Import order is the cascade: moving an import, or a rule between files, can change what renders.";
const SPLIT_POINTER = ["WHY", "stale line pointer (app.css:1885 does not exist) replaced by the rule itself", SPLIT];
export default {
  490: ["WHY", "ladder kept; the transparent-topbar history moved", "Surface ladder: --surface is the shell (sidebar and topbar, one L of chrome), --bg the canvas,\nand --surface again for a card on the canvas behind a border. The sidebar and a card never touch,\nso no fourth neutral is needed; rule 7 allows the one card, the posts index."],
  492: ["WHY", "trimmed", "The shared inset, as .site-header and every sidebar item use, so the mark's left edge matches on both\nplanes. Zero block padding centres the mark where .site-header puts its own."],
  493: ["WHY", "trimmed", "Ruling 8: on desktop the bar is already pinned, outside the scroller. sticky is for the narrow\nbreakpoint, where the document scrolls and the topbar would otherwise leave."],
  494: ["WHY", "shadow-not-border reason kept; the per-dpr measurements moved", "--border-strong, since content scrolls beneath this edge. A shadow, not a border: with border-box a\nborder shrinks the declared height and moves the mark, and device-pixel snapping makes the miss vary\nby display. A shadow keeps the bar exactly --admin-topbar-h, which .editor-canvas subtracts."],
  495: ["WHY", "grid-level min-width rule kept; the deployed measurement moved", "min-width: 0 on the grid item: its automatic minimum is min-content, so without this the track grows\nthe document and no truncation inside the bar ever engages."],
  496: ["WHY", "the yield order kept; overflow measurements moved", "The topbar's minimum width is its longest unbreakable token, the operator's email, so min-width: 0 lets\nit shrink. Three things yield: the email truncates, the wordmark truncates, the scope chip leaves.\nThe mark, drawer toggle and Sign out are controls and stay whole."],
  498: ["WHY", "trimmed", "The sign-out form is the flex item, so its refusal to shrink is declared here. Direct child only:\nthe folded menu renders a second sign-out form that is not a flex item of this bar."],
  499: ["WHY","fold reason and headroom rule kept; floor measurements moved; restored after Grok review 89352d6","The fold: below this width Sign out and the email move into the account menu (the existing overflow menu),\nbecause what remains after truncation is controls, and a control must not shrink past its label.\nNot 576: that existing breakpoint is below the measured 582px floor, so it cannot fold in time; 640 is the\nnearest existing one above it, with headroom because a longer address moves the floor."],
  500: ["WHY", "trimmed", "The folded block stops shrinking: its two controls would spill out. The wordmark truncates instead,\nwhile the mark keeps identifying the plane."],
  504: ["WHY", "trimmed", "Sign out is a control: its label never wraps or truncates. white-space, not flex, because the flex\nitem is the wrapping <form>."],
  505: ["WHY", "trimmed", "The secondary button weight (ruling 54), not a fourth shape. --border-strong, since the topbar is a\npinned surface (rule 5)."],
  506: ["WHY", "the fix and its token derivation kept", "WCAG 2.2 2.4.11: scroll-padding-top keeps focused controls from landing under the sticky topbar.\nDerived from --admin-topbar-h, never a literal that goes stale when the bar is resized. The editor's\ncommand bar sticks inside the scrollport and stacks below; the extra 1rem is breathing room."],
  507: ["WHY", "the plane-wide containing-block rule kept; the media-library measurement moved", "The scroller is also a containing block: an absolute .sr-only with no positioned ancestor is laid out in\ndocument coordinates and escapes this clip. This fixes it for every admin scroller at once."],
  508: ["WHY", "trimmed", "The editor manages its own height, so the content well stops scrolling and padding.\n:has(), not a route class, so the rule lives with the layout and no editor route can forget it."],
  511: ["WHY", "trimmed", "Ruling 8: the rail is a desktop affordance. Here the sidebar is an off-canvas drawer opened from the\ntopbar and dismissed by Escape or the backdrop; the collapsed attribute is ignored."],
  512: ["WHY", "the placement rule and its mechanism kept; the blank-page measurement moved", "The placement must reset with the track list: .admin-main asks for column 2, and in a one-track grid\nthat creates an implicit auto track a few pixels wide, blanking every admin page on a phone.\nNo gate sees layout (check:admin-ui renders without a stylesheet)."],
  514: ["WHY", "trimmed", "The ratified scrim pattern, not a new literal: opacity on --scrim, for the grounds at\n.media-detail-scrim in admin-media.css."],
  516: ["WHY", "the chip-leaves rule and nesting reason kept; the overflow arithmetic moved", "576px is where the topbar stops fitting (measured). The scope chip is the only purely ambient item, so it\nleaves rather than truncating. Nested in the 800px block because the toggle it makes room for exists\nonly there. Never visibility: hidden, which keeps the box."],
  520: ["WHY", "ratios are check:contrast's", "Every dot is ringed in its family's border token: a fill alone does not reliably clear 3:1 against\nthe page, so the fill is the identity and the ring the boundary."],
  521: ["WHY", "trimmed", "The origin-requests panel: tokens only, and no colour carries meaning alone."],
  527: ["WHY", "no-new-pair, prefers-contrast and focus rules kept; the redesign date moved", "The webmention queue. It adds no new contrast pair: filter chips reuse pairs check:contrast already\ncarries. No prefers-contrast rule and no focus rule here: app.css owns both tiers, and a rule here\nwould be a second owner."],
  528: ["WHY", "trimmed", "Both feedback boxes carry margin: 0, so the gap is declared once here; which box renders is the\naction's choice and the spacing is the same."],
  529: ["WHY", "trimmed", "The status filter is a row of links: every state is a URL, so filters need no script and history\nbehaves. Same shape as the media library's lens row."],
  532: ["WHY", "specificity trap kept", "The (0,1,0) versus (0,1,1) trap: a:visited outranks a class-only colour on an anchor, painting an\nunmeasured pair. Naming the states puts these at (0,2,0)."],
  533: ["WHY", "trimmed", "Three channels for the current filter (ruling 5): border, surface and weight. forced-colors keeps the\nweight and edge; aria-current carries it in every mode."],
  538: ["WHY", "trimmed", "A stranger's URL: overflow-wrap: anywhere, not break-all, so ordinary URLs still break at slashes.\nMonospace, because it is compared character by character. Still not a link."],
  541: ["WHY", "the ordering constraint kept", "Reject is the secondary (.btn-ghost), with geometry matched to the primary. Must come after\n.tool-row .btn-ghost, which has the same specificity and would win on font size."],
  543: ["WHY", "trimmed", "The third button weight, text only, named so Delete can sit on a row it is not the point of.\n1.75rem for the 24px target; a transparent resting border so hover moves nothing."],
  546: ["WHY", "the stacking reason kept; the dated measurement moved", "The row stacks on a phone: .mention-actions keeps its max-content width (flex-shrink: 0), which is wider\nthan the column. 640px is this sheet's existing narrow breakpoint. Delete moves to the far edge\n(ruling 21d) once the actions have their own row."],
  548: ["WHY", "trimmed", ".btn-text's shape comes from a background and a transparent border, which this mode does not keep,\nso a transparent outline (resolved to a system colour) keeps its edge."],
  549: ["WHY", "trimmed", "The active chip loses tint and hue here; the weight survives and the edge is restated in currentColor."],
  550: ["WHY", "trimmed", "Ruling 54: the admin page head, the one notice pattern, and the overview table. In the shell sheet,\nbecause every admin page opens with a title and one status sentence."],
  553: ["WHY", "trimmed", "The edge is the variant, never the fill: a tint competes on every load and spends rule 4's one tint.\nA 4px semantic border reads instantly and survives forced-colors."],
  563: ["WHY", "the no-opacity rule kept; the old declaration moved", "A token, never opacity: check:contrast reads token hexes, and opacity on text hides a failing composite\nbehind a passing pair. The count takes --text-muted, and on the current chip inherits --on-brand."],
  564: ["WHY", "measured sizes moved; the method and exception scope kept", "WCAG 2.2 2.5.8: a bare inline anchor's box is its line box and fell short of 24px. inline-flex with a\nmin-height grows the target and keeps both links on one centreline. The inline-link exception does not\napply to links alone in a nav."],
  565: SPLIT_POINTER,
  567: ["WHY", "the mirror admission kept; measurements moved", "The same column as the page head, chips and list. This restates their max-width and margin, a mirror\nthis repo normally refuses; the real fix is one shared class for all four."],
  569: SPLIT_POINTER,
  571: ["WHY", "trimmed", "One row, one centreline: every item takes --control-h, so no item's height sets the row's. Declared here,\nnot in the token blocks, because only these controls share it and check:contrast parses :root as colours."],
  573: ["WHY", "specificity and no-visited rules kept; the proof note moved", "Nav links are bare text, so they underline (rule 2); the search icon opts out on its own rule. Nav links\nhave no :visited state: wayfinding is not a trail. .site-header-nav a:visited is (0,2,1) against\na:visited at (0,1,1), so this wins rather than ties."],
  574: ["WHY", "trimmed", "The text link gets the controls' hit area, so its baseline sits on their centreline and the underline\nstays still. :not() because the search control is also an anchor and sizes itself."],
  575: ["WHY","the unmeasured-pair rule kept; the pressed-state date moved","The pill controls use the chrome's tokens (--on-chrome, --on-chrome-muted): --border, --text-muted and\n--tint-brand vanish or blotch on purple. Hover is a colour step, not a fill: nothing in the header is filled."],
  576: ["WHY", "the one-visible-button mechanism and specificity kept; the order history moved", "Theme toggle: two buttons ship and one is displayed; the other is display: none, so a screen reader finds\none. With no choice, prefers-color-scheme picks, matching the no-script form. html[data-theme] must outrank\nthe media query, and does: (0,3,1) against (0,2,0). Script only sets the attribute. No transition."],
  581: ["WHY", "the two-owner split, the build requirement and the no-colour rule kept", "The site's half of the math stylesheet, hand written; build-katex.mjs appends it to the generated\nupstream CSS. Run npm run build:katex after editing: check:content byte-compares the output.\nShips only with math. Sets no colour: KaTeX draws in currentColor, so themes and forced-colors follow."],
  582: ["WHY", "scroll, overflow-y, focus and margin whys kept", "Display math scrolls inside itself: KaTeX sets nowrap, so a long equation is wider than the column.\noverflow-y: hidden avoids a second scrollbar; the padding keeps subscripts. Not focusable, like\n.prose pre; change both together or neither. Margin matches .prose pre, since upstream's em is wider."],
  584: ["WHY", "trimmed", "The print rule colour lives here only. Never a theme token: check:contrast requires every theme token\nin a measured screen pair, and this colour exists only on paper."],
  587: ["NUMBER","frame counts and durations are a dated measurement; check:browser asserts the null viewTransition; restored after Grok review 89352d6","The public plane opts out of cross-document view transitions: Chrome's default crossfade showed both pages\nstacked on every header click. With it off, paint holding keeps the old page until the new one paints.\nNever bring back a reduced-motion block that skips the fade but leaves the transition on: it protects only\nreduced-motion readers and leaves everyone else with the blink. check:browser asserts a null viewTransition."],
  588: SPLIT_POINTER,
  591: ["WHY", "trimmed", "The shell owns the viewport so the editor owns one scroll context; a scrolling body behind it would be\nthe forbidden double scroll. Other pages scroll .admin-content instead."],
  593: ["WHY", "history of two spellings moved", "The ratified scrim, one owner for the role."],
  594: ["WHY", "trimmed", "WCAG 2.4.7: the input takes focus when the palette opens, so it needs a ring. :focus-visible, not :focus.\nThe on-surface ring token, so it stays inside what the contrast gate reads."],
  595: SPLIT_POINTER,
  596: ["WHY", "trimmed", "/playground: controls and results sit on --surface while prose stays on the canvas (rule 7).\nNo colour carries meaning alone: pass and fail are words."],
  603: ["NUMBER", "the overflow measurement has no gate; the why stays", "Tight padding so the six columns fit the measure and the fused total is never clipped.\nIt still scrolls if a title forces it to."],
  608: ["WHY", "trimmed; the audit tier note moved", "Code block furniture is script-added, so its room is reserved only when script can run:\nscripting: enabled at first paint, or [data-enhanced] after. With no script there is no gap.\nposition: relative stays unconditional; it reserves nothing."],
  609: ["WHY", "trimmed", "Highlighted lines use a neutral band, not a brand tint: check:contrast holds syntax tokens to 4.5:1\nonly against the code surfaces. The brand shows as the left bar, where no text sits."],
  610: ["WHY", "top-layer reason kept; the date moved", "A native modal <dialog>: it is in the top layer, above every stacking context, so it needs no z-index.\nThe scrim is ::backdrop, as in the palette."],
  612: ["WHY", "trimmed", "The close button makes dismissal discoverable. Positioned against the dialog, which is why the dialog\nkeeps overflow: visible. --border-strong, never --border, which does not hold over a scrim (hard rule 5)."],
  614: ["WHY", "trimmed", "Further reading, related and mentions share selectors, so the rhythm has one owner. Mentions adds only\ngaps between entries and no bullet. No new colour token."],
};
