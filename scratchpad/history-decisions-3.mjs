// Ruling 115 decisions, blocks 357 to 487. Same shape as history-decisions-1.mjs.
const SPLIT = "Import order is the cascade: moving an import, or a rule between files, can change what renders.";
export default {
  357: ["WHY", "mockup ratios moved", "A column heading is a link, because its sort is a URL. Quiet by size, tracking and uppercase,\nnever by an unreadable grey."],
  360: ["WHY", "the positional-selector prohibition kept; the first-render defect moved", "A numeric heading sits over the edge its values align to and reads its side off its own attribute.\nNever nth-of-type here: it counts same-type siblings, and this row mixes anchors and spans."],
  361: ["WHY", "the reset reason kept; the measured slab moved", "The reset is load-bearing: .media-card paints --surface on every card, so without it the odd-row\nbanding below paints --surface onto --surface and the rows become one slab."],
  362: ["WHY", "seam rule kept; the ratios are measurements", "Rows separate by a full-width tint band, not a hairline: the mockup's hairlines fail their floor.\nA surface-to-surface seam carries no 3:1 obligation under the palette law."],
  366: ["WHY", "trimmed", "The name clips rather than wrapping: one wrapped name knocks the whole row out of alignment with the\nheader. The full name is in the title and the inspector, and the directory beneath carries the rest."],
  369: ["HISTORY", "records a removed rule; nothing in the sheet depends on it", null],
  373: ["WHY", "column-drop rules kept; the width arithmetic moved", "Narrow viewports: the fixed tracks do not fit a phone, so columns drop rather than the table scrolling,\nwhich would need header and groups to scroll together. Dims and Added go first; Size stays.\nThe header cells drop in the same query: a heading over a removed column is worse than none."],
  375: ["WHY","trimmed","Draft preview links, in the drawer: popover step, so --border-strong, never --border (hard rule 5).\nNot a .slug-line: the meta column must shrink while two actions keep their width."],
  382: ["WHY", "cascade rule kept; split history moved", "The admin posts index: toolbar, filters, overflow menu, disabled affordance, bulk bar.\n" + SPLIT],
  385: ["WHY", "trimmed", "Search and filter: a wrapping row, not a grid, because the fields have different natural widths.\nOnly the search field grows."],
  393: ["WHY", "trimmed", "Overflow menu. overflow-menu.tsx says why it is a <details> disclosure, not role=\"menu\"."],
  396: ["WHY", "ratios are check:contrast's", "--text-muted is measured on --surface-popover but not on the brand tint a hovered row paints,\nso the hovered row drops the hint to body text, which is measured there."],
  399: ["WHY", "ratios are check:contrast's", "The border is the lighter family token, not the fill again: in dark mode a same-colour border\ngives the crimson button no edge, and --border-danger does."],
  400: ["WHY", "specificity trap and the Link rule kept; the measured failure ratios moved", "The specificity trap: anchors wearing these classes are reached by a:visited and a:hover at (0,1,1),\nwhich outrank a class at (0,1,0) and paint page-measured text on the fill. Naming the link states puts\nthese at (0,2,0). Any filled or bordered control worn by a Link needs the same; check:contrast owns the pairs."],
  401: ["WHY", "the found-live date moved", "Disabled affordance: WCAG exempts a disabled control from contrast, so dimming is allowed here, but the\nreader must still see it is unavailable. Opacity AND cursor, so it survives forced-colors."],
  403: ["WHY", "trimmed", "Exempt from rule 2, like .btn: the border is the affordance. Without this, ghost controls that are\nLinks underline and buttons do not."],
  404: ["WHY", "trimmed", "The posts index is a card (rule 7: indexes are what cards are for), the third rung of the surface\nladder: shell chrome, canvas, card."],
  406: ["WHY", "trimmed", "The bulk bar exists only while something is selected. A pinned bar on the popover step, so its seam is\n--border-strong, never --border (rule 5); inside the card above the table, so only the bottom edge is drawn."],
  408: ["WHY", "trimmed", "24px minimum target (WCAG 2.2 AA, 2.5.8): the label carries the hit area with the input inside,\nand it also names the control, so no aria-label can drift from it."],
  410: ["WHY", "the no-sideways-scroll rule and min-width reason kept; the deployed measurement moved", "The table's own scrollport: the document must never scroll sideways; a wide table may, inside its box.\nmin-width: 0 because this is a flex item, and otherwise the scrollport is as wide as the table."],
  411: ["WHY", "the containing-block rule kept; the measured displacement moved", "A scroller must also be a containing block: .sr-only is absolute with no offsets, and a containing block\noutside the scroller lets it drag the document sideways. position: relative moves it here."],
  413: ["WHY", "trimmed", "Hover is the neutral elevation step, never the brand tint: a row under a pointer is not a choice, and\n--text-muted is measured on --surface-popover, not on the brand tint."],
  414: ["WHY", "ratios are check:contrast's", "The separator steps up with the surface: on the popover step --border falls below the 3:1 UI floor\nin dark, so a hovered row with the default border would fail AA."],
  415: ["WHY", "trimmed", "Status pill: three states from the two stored (see the loader). The word is the first channel;\nborder style is the second, because forced-colors keeps strokes. Published solid, scheduled dashed,\ndraft dotted."],
  416: ["WHY", "trimmed", "Rule 5: a state that is true now takes a fill. The ring is the lighter family --border-* token, which\ngives the pill an edge, as .btn-danger does."],
  422: ["WHY", "trimmed", "Row-action forms, parked below the table and targeted by form attributes, since forms cannot nest.\ndisplay: none, as nothing here is content; form association ignores display."],
  423: ["WHY", "trimmed", "Clears the WCAG 2.2 24px target minimum. --border-strong because the row elevates to\n--surface-popover on hover, where --border fails in dark."],
  425: ["WHY", "trimmed", "Standing-condition alert (alert.tsx says why it is a named region, not a live one). Amber: drift is a\ndegradation, not a failure. It is the view's one semantic tint (rule 4), and the scheduled pill shares\nthe family so the two never sit side by side in different ones."],
  430: ["WHY", "trimmed", "Ruling 54: the posts list to docs/ADMIN-DESIGN.md. Two radii only, both on .admin in admin-shell.css:\n--r-control on anything pressed, --r-panel on anything that contains."],
  436: ["WHY", "specificity reason kept", "Specificity matched to the reveal: a bare .posts-bulk { display: none } would win only by source order,\nand moving one import would show the bar with nothing selected. Both halves are scoped to the card."],
  445: ["WHY", "trimmed", "The no-script step: data-inline is on the server render and removed when showModal() runs.\nWithout it a dialog with no open attribute is display: none, and a scriptless reader loses the confirmation."],
  446: ["WHY", "trimmed", "A disabled danger button keeps its fill (ruling 54): the reader must see which red button they are\nstopped from pressing. Overrides the shared disabled opacity."],
  449: ["WHY", "cascade rule kept; split history moved", "The admin cockpit frame: sidebar, nav, badges, topbar, panels, origin-requests panel.\n" + SPLIT],
  450: ["WHY", "the broken-transition prohibition kept; the dated measurement moved", "auto, not the width: the width lives on .admin-sidebar. Never transition grid-template-columns from a\ncustom property; the computed template freezes. A real element's width interpolates dependably."],
  451: ["WHY", "trimmed", "A header row spanning both columns, so the mark's x never depends on the sidebar's width."],
  452: ["WHY", "trimmed", "The shell's lengths, scoped to .admin because check:contrast parses the token block as colours.\nThe topbar height offsets the narrow breakpoint; the canvas subtracts the command bar height,\nso it must be a number; one control height serves the whole command bar."],
  453: ["NUMBER", "the measured header height derivation has no gate; the load-bearing note stays", "4.25rem, not 4rem: it matches the public header's rendered height, not its declared minimum, so the\nmark centres at the same y. Load-bearing: the editor canvas and narrow bar offsets read it."],
  454: ["WHY", "trimmed", "The expanded width is from the ruling; the collapsed width is derived from the inset. The inline script\nsets the attribute before first paint, so the rail never flashes wide."],
  455: ["WHY", "derivation kept; the before-state offsets moved", "The shared icon column: the topbar mark and every sidebar icon share one column and gap, so labels and\nwordmark start at one x. Its width is the mark's own (2rem at the crop's 36x50 aspect), written as the\narithmetic so the aspect stays visible."],
  457: ["WHY", "trimmed", "The rail is derived, never a literal: inset + column + inset, so the icons do not move when it closes.\nA literal would silently stop matching the first time the inset is tuned."],
  459: ["WHY", "trimmed", "The two radii (ruling 54), and there is no third: --r-control on anything pressed, --r-panel on\nanything that contains. check:admin-ui refuses a stray radius."],
  460: ["WHY", "the no-transition rule and its circularity kept; the dated measurements moved", "No width transition, which satisfies ruling 6's reduced-motion ask and is a correctness choice: the\nsidebar is a grid item in an auto track, so animating its width is circular and stalls. Only the\nchevron, a transform, animates."],
  461: ["WHY", "trimmed", "No left padding: the inset lives on the item, so the active rule sits flush against the rail's edge\nand the item reads as a tab attached to the column."],
  462: ["WHY", "trimmed", "A shadow, not a border: with border-box a border is subtracted from the width, and the collapsed\nrail's width is the alignment."],
  464: ["WHY", "trimmed", "The foot is a zone, marked by a hairline. --border, not --border-strong: the sidebar is the shell,\nnot an elevated surface."],
  465: ["WHY", "trimmed", "One item shape, shared with the topbar brand block: a 44px target, the shared icon column and gap,\na label the rail hides. The inset is all padding; the active rule is out of flow."],
  467: ["WHY", "the border-or-alignment rule kept; the measurement moved", "A border may take part in an alignment or be a fixed length, not both: Chrome snaps borders to device\npixels, so subtracting the declared width cannot cancel the rendered one."],
  468: ["WHY", "trimmed", "The glyph's box is the shared column, so it centres where the mark does; the square viewBox lets\npreserveAspectRatio centre it with no wrapper."],
  469: ["WHY", "trimmed; the mockup ratio moved", "The section count is a size, not a badge: no fill or pill, just the right edge and muted text, so it\nnever reads as an alarm. margin-left: auto, inboard of any drift badge. Tabular figures."],
  471: ["WHY", "trimmed", "In the collapsed rail the count goes and the badge stays: a number with no unit is unreadable.\nThe count survives in the aria-label, and title carries it for a pointer."],
  472: ["WHY","trimmed","Ask index drift, on the item that owns the repair. A fill (interactive semantics take fills, never\npastel text); warning, not danger, since a stale corpus degrades an enhancement. The numeral is\naria-hidden; the name says it in words."],
  473: ["WHY", "trimmed", "In the rail the badge stays a numeral at the icon's top right: it pulls the eye to Posts, and the rail\nis when the label is gone. title carries the words."],
  474: ["WHY", "trimmed", "The collapsed rail: labels go, targets stay. The name is an aria-label, so hiding the text is safe;\ndisplay: none on a label that was the name would leave the item nameless."],
  475: ["WHY", "derivation reason kept; the amendment's before-state moved", "The rail keeps the shared column: it is sized from the inset, so one padding-left serves both states\nand nothing is centred. Never justify-content: center, which would tie the icon's x to the right\npadding and border."],
  478: ["WHY", "the override prohibition kept; measured offsets moved", "Height only: the width is the shared column from .admin-nav-icon, and overriding it misaligned the\ntoggle's glyph. Slightly smaller, so it reads as chrome, not a sixth section."],
  480: ["WHY", "specificity trap kept; the tally moved", "The identity block, as .site-header-brand. The :visited selector is needed: .admin-brand is (0,1,0)\nand a:visited is (0,1,1), so without it the wordmark turns claret. Any identity element or filled control\nworn by a Link needs the same. No underline: identity is not a destination."],
  481: ["WHY", "the measured widths moved", "min-width: 0 on the flex item, not just the text: nowrap raises its min-content to the whole wordmark,\nso truncating the name alone widens the bar."],
  482: ["WHY", "trimmed", "The shared gap, so the wordmark and the nav labels start at the same x.\nIt equals .site-header-brand's, so the public parity holds."],
  483: ["WHY", "trimmed", "Same height, display and width as .site-header-mark, so the mark scales identically on both planes.\nThe component is imported, so check:logo covers it."],
  484: ["WHY", "trimmed", "The shared column, stated rather than left to width: auto; it is the mark's natural width, so parity\nholds and the column is declared once."],
  486: ["WHY", "trimmed", "The active rule is a border on an absolutely positioned box, out of flow, so items keep their inset.\nA border, not a background bar: forced-colors overrides backgrounds but re-colours borders (ruling 5)."],
  487: ["WHY", "trimmed", "View site: nav item treatment. No underline: the leaving-arrow glyph is its non-colour affordance,\nand the accessible name says it leaves the plane."],
};
