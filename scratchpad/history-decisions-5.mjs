// Ruling 115 decisions, blocks 616 to 699. Same shape as history-decisions-1.mjs.
const SPLIT = "Import order is the cascade: moving an import, or a rule between files, can change what renders.";
const SPLIT_POINTER = ["WHY", "stale line pointer (app.css:1885 does not exist) replaced by the rule itself", SPLIT];
export default {
  617: SPLIT_POINTER,
  618: ["WHY", "measured sizes and audit note moved", "WCAG 2.2 2.5.8: a bare anchor's box is its line box, which fell short of 24px here,\nso these wide links get an explicit minimum height rather than relying on their width."],
  625: SPLIT_POINTER,
  626: ["WHY", "the start-alignment reason kept; the ruling date moved", "Projects index: cards on the canvas (rule 7 permits it for an index); the metric leads, above the name.\nauto-fit with a minimum, so no breakpoint. align-items: start, so a card ends where its content ends\ninstead of stretching to the tallest; the foot margins still order links above evidence."],
  629: ["WHY", "trimmed", "Technical notes are prose in a card: muted body colour, the description's measure, and no bullets,\nwhich would promise an order the sentences do not have."],
  632: ["WHY", "trimmed", "Pushed to the foot: an auto top margin works only on the last flex child, so the evidence block takes it\nwhen present and this otherwise."],
  637: ["WHY", "trimmed", "The blurred placeholder behind a prose image: the pipeline writes has-lqip and an inline background image;\neverything shared is here. No transition or cleanup: the opaque image covers it (exclude transparent\nimages at derivation). width and height are always set, so the box is final before paint."],
  638: ["WHY", "trimmed", "Body images link to their original. Exempt from rule 2's underline, since the whole content is an image.\nzoom-in is honest with or without the lightbox."],
  639: ["WHY", "the no-opacity rule kept; the dated composite ratios moved", "No opacity on this text: opacity composites after check:contrast reads the token, so a passing pair can\nship below the 4.5:1 floor. Size separates the credit from the caption."],
  640: ["WHY", "trimmed", "Charts: the renderer strips Plot's injected <style>, which carried a white literal per chart; these rules\nreplace it once. Axes inherit --text via currentColor; series use var(--chart-*), so one SVG serves both\nthemes."],
  643: ["WHY", "trimmed", "Diagrams are build-time <img> assets (mermaid needs a real browser; see dustinedwards/chart-stack.md).\nTwo images, one per theme, since custom properties never reach an SVG in <img>. display: none, which\nalso drops the hidden one from the accessibility tree. Selectors follow the site's theme resolution."],
  644: ["WHY", "the anywhere-not-break-word rule kept; the CI finding moved", "Inline code may break: its contents are single unbreakable tokens, and monospace fallbacks differ in width\nby platform. anywhere, not break-word: only anywhere lowers min-content, so a token cannot widen the page.\nIt breaks only when it must, so short spans are unchanged."],
  645: ["WHY", "trimmed", "The :swatch chip. The label is a real <code>, so the inline-code rule already styles it; a second\ndeclaration would drift. No per-theme block: the border is --border and the fill is the post's own hex."],
  650: ["WHY", "trimmed", "Flattened in forced-colors on purpose (the palette yields, per v3). Never forced-color-adjust: none.\nThe hex survives as text beside the chip; the border is repainted in CanvasText."],
  654: ["WHY", "trimmed", ".sr-only lives only in reset.css: a second copy here would make the class behave differently on pages\nwith and without prose. One owner, rule 17."],
  656: ["WHY", "measured size moved", "WCAG 2.2 2.5.8 and 1.4.3. inline-flex with a min-width, not padding, which would push the anchor away\nfrom its heading; the heading's line box gives the height."],
  657: ["WHY", "the no-partial-opacity rule kept; the old values and date moved", "The reveal is scoped to hover-capable pointers and goes 0 to 1, never a partial opacity, which composites\nafter check:contrast measures. On touch the anchor is plain --text-muted. focus-visible is safe inside\nthe query: outside it the anchor is already visible."],
  658: ["WHY", "trimmed", "Shiki dual themes: both palettes ride as custom properties, so switching needs no script. The theme's own\nbackground is dropped; blocks sit on --surface-code, where check:contrast holds every syntax token to\n4.5:1 (and on --surface-popover). With no attribute :root:not([data-theme]) answers; no light override."],
  660: ["WHY", "trimmed", "Diff scopes and carriage-return pair a foreground with their own background and are only legible on it,\nso those spans keep it. The attribute test matters: custom properties inherit."],
  661: SPLIT_POINTER,
  662: ["WHY", "seam and scope rules kept; the provisional-review provenance and ratios moved", "Public header: the chrome is a brand surface in both themes. The seam takes --border-strong, never --border:\na seam needs no 3:1, but it needs a visible edge. Public plane only."],
  663: ["WHY", "the wrap rule on both elements kept; the overflow measurement moved", "The header wraps, or public pages scroll sideways on a phone. flex-wrap is on both .site-header and\n.site-header-nav: on either alone, one of them stays an unbreakable row. No media query."],
  666: ["WHY","the identity exemption, specificity and the do-not-simplify prohibition kept verbatim; ruling date and ratios moved","Identity exemption: the wordmark never takes :visited. .site-header-brand is (0,1,0) and a:visited\n(0,1,1), so without these it turns claret. It takes --on-chrome; hover steps to --on-chrome-muted.\nThe :visited and :hover selectors are (0,2,0) and outrank the base pseudo-class rules deliberately.\nDo not \"simplify\" them away. No colour pseudo-class reaches the mark."],
  669: ["WHY", "the separate-sheet reason kept; the restore provenance moved", "The publications index and per-paper pages. A separate sheet because page styles left app.css;\nits colours use the Hill Country token names."],
  673: ["WHY", "trimmed", "Never opacity: check:contrast cannot see an alpha applied after the declared pair.\nThe count uses the chip's own colour, a measured pair."],
  676: ["WHY", "date moved", "One paper's page. It reuses .pub-badge, .pub-links, .pub-chip and .pub-author-me:\nsame facts at a different density, and a second set of names would drift."],
  677: ["WHY", "the corpus figure moved", "Smaller than other page titles on purpose: a paper title is a sentence and runs to several lines."],
  679: ["WHY", "the entry count moved", "Who cites this paper: a numbered list of sentences (title, venue, year), newest first."],
  682: ["WHY", "the tense-bound state claim removed", "A retraction, correction or concern notice. Tinted, unlike .paper-summary: the publisher is contradicting\nthe document, so it must be seen first. The warning trio is already a measured contrast pair in both themes."],
  683: ["WHY", "the reset's role and the @layer prohibition kept (Grok's restored point); vendoring census moved", "The base reset, vendored from Tailwind's preflight, with font tokens swapped in. It is load-bearing:\nremoving it moves computed values on every page. The @layer base wrapper must stay gone: nothing else is\nlayered, so it would only change which rules win. Imported first in app.css, so site rules win by position."],
  685: ["WHY", "trimmed", "Hidden stays hidden: the browser's [hidden] loses to a class rule, and the search hint carries both\nhidden and .sr-only. This one does not lose, so the hint stays out of the accessibility tree."],
  686: ["WHY", "the one-edit note kept; the post.css history moved", ".sr-only, the one Tailwind utility the site used, copied so computed values do not move.\nOne edit: border: 0, which is what shipped."],
  690: ["WHY", "trimmed; the ruling date moved", "Search entry point: one icon control, no visible \"/\". The shortcut is announced through title and\naria-describedby, written by the enhancement, so it exists only with the listener. No text plus a border\nmeets rule 2. Height is --control-h; the padding keeps it square above the 24px target."],
  691: ["WHY","the bar-padding prohibition kept (Grok's restored point); dated removals moved","The track grid and the footer; public-chrome.css and chrome-nav.css own the header. Do not restore the\nbar padding \"just in case\": against a static header it is a blank band atop every page. Imported last in\nroot.tsx, a list that is the cascade and must not be sorted; it no longer needs to outrank the chrome sheets."],
  692: ["WHY", "the .tracks-not-.page rule kept; the canvas sentinel and date moved", "1 · The track grid, named .tracks, not .page: .page has live consumers, and one name for two jobs drifts.\nThe track is the measure, so a full-bleed figure and a paragraph share one source. At narrow widths the\n1fr tracks are zero and all three share a column, so no media query."],
  693: ["WHY", "the prohibition kept; the check:browser measurement moved", "100% minus the gutters, never 100%: 100% is the whole grid container, so the fixed tracks plus gutters\noverflowed at narrow widths."],
  694: ["WHY", "trimmed", "Single-direction flow spacing: grid does not collapse margins, so only start margins exist.\nA heading binds to what follows: .tracks > h2 + * is (0,1,2) against .tracks > figure at (0,1,1)."],
  696: SPLIT_POINTER,
  697: ["WHY", "phase label moved", "Enhancement styles: none are needed to read the page, and every animation is off under\nprefers-reduced-motion."],
  698: ["WHY", "trimmed", "Skip link, visible on focus at top 0, so it is seen against the chrome, where a --brand fill vanishes.\nIt takes the chrome's inverted pair. The progress bar keeps --brand: it has width only after the chrome\nhas scrolled away."],
  699: ["WHY", "ratio moved", "Its inset ring: the pale fill needs a different ring. At (0,1,1) it ties the base inset rule and wins\non order, so it must stay below that block."],
};
