name: improve-score

# THE SCORER, SPLIT INTO TWO JOBS (audit 2026-09-06, option a). dustinedwards-info copy.
#
#   build  (PER REPO, no secrets) runs this repo's own build/test/lint/measure and
#          writes a normalized metrics.json plus the attempt source under code/.
#   score  (BYTE-IDENTICAL across all five roster repos) overlays the source, pulls
#          the holdout, counts passes from node --test's TAP reporter via
#          scripts/improve-report.mjs (never an exit code), reads metrics.json,
#          signs and posts.
#
# ONLY THE build JOB DIFFERS PER REPO. The score job and scripts/improve-report.mjs
# are copied verbatim from capsid-mcp; edit them there and re-copy.

on:
  workflow_dispatch:
    inputs:
      branch:
        description: The attempt branch to score.
        required: true
      run_id:
        description: The improve run this attempt belongs to.
        required: true
      attempt_id:
        description: The attempt being scored. The literal suffix -baseline measures the base.
        required: true

permissions:
  contents: read

jobs:
  build:
    name: build (per-repo, no secrets)
    runs-on: ubuntu-latest
    timeout-minutes: 25
    outputs:
      head_sha: ${{ steps.head.outputs.sha }}
    steps:
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5
        with:
          ref: ${{ inputs.branch }}
          persist-credentials: false

      - name: Capture the attempt head sha
        id: head
        run: echo "sha=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"

      - name: Stamp the start
        id: started
        run: echo "epoch=$(date -u +%s)" > started.txt

      - uses: actions/setup-node@a0853c24544627f65ddf259abe73b1d18a591444 # v5
        with:
          node-version-file: .nvmrc
          cache: npm

      - name: Install dependencies
        run: npm ci

      # ---- PER-REPO. dustinedwards-info: react-router, check-all.mjs, oxlint. ----
      - name: Build
        id: build
        continue-on-error: true
        run: npm run typecheck

      - name: Test
        id: test
        continue-on-error: true
        run: npm run check:ci 2>&1 | tee test-output.txt

      - name: Measure
        id: measure
        continue-on-error: true
        run: |
          set -uo pipefail
          BUILD_PASSES=0; [ "${{ steps.build.outcome }}" = "success" ] && BUILD_PASSES=1
          # check-all.mjs prints "N passed, M failed".
          RATE=$(node -e '
            const fs=require("fs");
            try {
              const m=/(\d+) passed, (\d+) failed/.exec(fs.readFileSync("test-output.txt","utf8"));
              if(!m){process.stdout.write("");}
              else{const p=Number(m[1]),f=Number(m[2]);process.stdout.write(p+f>0?String(p/(p+f)):"");}
            } catch { process.stdout.write(""); }
          ')
          # oxlint --deny-warnings has no count to read; left null.
          LINT_COUNT=""
          BUNDLE_BYTES=$(npm run build > /dev/null 2>&1 && du -sb build 2>/dev/null | cut -f1 || echo "")
          BUILD_PASSES="${BUILD_PASSES}" RATE="${RATE}" LINT_COUNT="${LINT_COUNT}" BUNDLE_BYTES="${BUNDLE_BYTES}" node -e '
            const n=(v)=>(v===undefined||v===""?null:Number(v));
            process.stdout.write(JSON.stringify({
              build_passes: Number(process.env.BUILD_PASSES||"0"),
              test_pass_rate: n(process.env.RATE),
              lint_count: n(process.env.LINT_COUNT),
              bundle_size_bytes: n(process.env.BUNDLE_BYTES),
            }));
          ' > metrics.json
          cat metrics.json

      # PER-REPO: the source tree(s) the holdout imports. dustinedwards-info: app, workers.
      - name: Stage the scored source
        run: |
          set -euo pipefail
          mkdir -p code
          cp -r app code/
          cp -r workers code/

      - name: Upload the attempt artifact
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: attempt
          retention-days: 1
          path: |
            metrics.json
            started.txt
            code

  # ============================================================================
  # BYTE-IDENTICAL BELOW THIS LINE ACROSS ALL FIVE ROSTER REPOS. Do not edit one
  # copy; edit capsid-mcp's and re-copy.
  # ============================================================================
  score:
    name: score (holds secrets)
    needs: build
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5
        with:
          persist-credentials: false

      - uses: actions/setup-node@a0853c24544627f65ddf259abe73b1d18a591444 # v5
        with:
          node-version-file: .nvmrc
          # Off because it would auto-run the package manager to locate a cache dir
          # before corepack has provisioned pnpm for a pnpm-workspace repo.
          package-manager-cache: false

      # PACKAGE-MANAGER AGNOSTIC so this job stays byte-identical across an npm repo
      # and a pnpm workspace: pnpm-lock.yaml picks pnpm (via corepack, honouring the
      # repo's packageManager field), otherwise npm ci. No setup-node cache, because
      # a cache key would have to name one manager.
      - name: Install dependencies
        run: |
          set -euo pipefail
          if [ -f pnpm-lock.yaml ]; then
            corepack enable
            pnpm install --frozen-lockfile
          else
            npm ci
          fi

      - name: Download the attempt artifact
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
        with:
          name: attempt
          path: attempt

      - name: Overlay the attempt source
        run: |
          set -euo pipefail
          # Path-preserving: Job A stages each scored tree under code/ keeping its
          # full path (e.g. code/src, code/packages/core), so copying code/. back to
          # the repo root lands each tree where the holdout imports it, nested dirs
          # included. Byte-identical across all five repos; only what Job A stages
          # differs.
          if [ -d attempt/code ]; then
            cp -r attempt/code/. .
          fi

      # R2 holdout secrets are env ON THIS STEP ONLY; no attempt code runs here.
      # The aws CLI reads only the AWS_* names, so the secrets are mapped onto
      # them (R2_ACCESS_KEY_ID was invisible to it: every sync failed with
      # "Unable to locate credentials" and the holdout scored 0). Checksums are
      # when_required because R2 rejects the CRC headers newer aws CLIs send by
      # default.
      - name: Pull the holdout suite
        id: holdout
        continue-on-error: true
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.IMPROVE_HOLDOUT_R2_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.IMPROVE_HOLDOUT_R2_SECRET_ACCESS_KEY }}
          AWS_DEFAULT_REGION: auto
          AWS_REQUEST_CHECKSUM_CALCULATION: when_required
          AWS_RESPONSE_CHECKSUM_VALIDATION: when_required
          R2_ACCOUNT_ID: ${{ secrets.IMPROVE_HOLDOUT_R2_ACCOUNT_ID }}
          IMPROVE_NAMESPACE: ${{ vars.IMPROVE_NAMESPACE }}
        run: |
          set -euo pipefail
          mkdir -p .improve-holdout
          aws s3 sync \
            "s3://capsid-improve-holdout/improve/holdout/${IMPROVE_NAMESPACE}/" \
            .improve-holdout/ \
            --endpoint-url "https://${R2_ACCOUNT_ID}.r2.cloudflarestorage.com"

      # Count passes from the TAP reporter via the trusted script, never exit codes.
      - name: Run the holdout suite
        id: holdout_run
        continue-on-error: true
        run: |
          set -uo pipefail
          TOTAL=$(node -e 'try{process.stdout.write(String(require("./.improve-holdout/manifest.json").total))}catch{process.stdout.write("")}')
          LOADER=""
          [ -f ./test/resolve-ts.mjs ] && LOADER="--import ./test/resolve-ts.mjs"
          PASSED=0
          for f in .improve-holdout/*.test.*; do
            [ -e "$f" ] || continue
            REPORT=".improve-holdout/.report-$(node -e 'process.stdout.write(require("crypto").randomUUID())').json"
            # shellcheck disable=SC2086
            node --test --test-reporter=tap --test-reporter-destination="$REPORT" $LOADER "$f" > /dev/null 2>&1 || true
            if node scripts/improve-report.mjs --holdout "$REPORT"; then
              PASSED=$((PASSED+1))
            fi
          done
          echo "total=${TOTAL}" >> "$GITHUB_OUTPUT"
          echo "passed=${PASSED}" >> "$GITHUB_OUTPUT"

      # RUN_ID, ATTEMPT_ID and the key are in THIS step's own env so a poisoned
      # GITHUB_ENV cannot override them; NODE_OPTIONS is cleared; the HMAC is
      # computed by node with the key read from the environment, never argv; curl is
      # absolute-pathed. ci_minutes is computed before the body.
      - name: Post the score report
        if: always()
        env:
          IMPROVE_SCORE_KEY: ${{ secrets.IMPROVE_SCORE_KEY }}
          IMPROVE_NAMESPACE: ${{ vars.IMPROVE_NAMESPACE }}
          CAPSID_URL: https://capsid.dustin-edwards.workers.dev
          RUN_ID: ${{ inputs.run_id }}
          ATTEMPT_ID: ${{ inputs.attempt_id }}
          ATTEMPT_HEAD_SHA: ${{ needs.build.outputs.head_sha }}
          HOLDOUT_TOTAL: ${{ steps.holdout_run.outputs.total }}
          HOLDOUT_PASSED: ${{ steps.holdout_run.outputs.passed }}
          NODE_OPTIONS: ""
        run: |
          set -euo pipefail
          if [ -z "${IMPROVE_SCORE_KEY}" ]; then
            echo "IMPROVE_SCORE_KEY is unset; the report cannot be signed and will not be sent." >&2
            exit 1
          fi
          STARTED_EPOCH=$(sed -n 's/^epoch=//p' attempt/started.txt 2>/dev/null || echo "")
          export CI_MINUTES=$(S="${STARTED_EPOCH}" N="$(date -u +%s)" node -e 'const s=Number(process.env.S||"0"),n=Number(process.env.N||"0");process.stdout.write(s>0?((n-s)/60).toFixed(2):"0")')
          BODY=$(node scripts/improve-report.mjs attempt/metrics.json "${HOLDOUT_TOTAL}" "${HOLDOUT_PASSED}")
          TS=$(date -u +%Y-%m-%dT%H:%M:%SZ)
          SIG=$(TS="${TS}" BODY="${BODY}" node -e 'const c=require("crypto");const h=c.createHmac("sha256",process.env.IMPROVE_SCORE_KEY);h.update(process.env.TS+"."+process.env.BODY);process.stdout.write(h.digest("hex"))')
          echo "Posting score report for ${ATTEMPT_ID}:"
          echo "${BODY}"
          # A shakedown dispatch (the literal RUN_ID "shakedown") has no run row in
          # the Worker, so its report is answered 409 "unknown run" by design, AFTER
          # the signature verified (a bad signature is 401 before the run lookup).
          # That one answer, on that one RUN_ID, is success: it proves the pipeline
          # end to end. Any other non-2xx, and a 409 on a real run id, still fails
          # this step loudly.
          RESPONSE_FILE=$(mktemp)
          STATUS=$(/usr/bin/curl --silent --show-error -o "${RESPONSE_FILE}" -w '%{http_code}' \
            -X POST "${CAPSID_URL}/improve/score" \
            -H "Content-Type: application/json" \
            -H "X-Improve-Namespace: ${IMPROVE_NAMESPACE}" \
            -H "X-Improve-Timestamp: ${TS}" \
            -H "X-Improve-Signature: ${SIG}" \
            --data "${BODY}")
          cat "${RESPONSE_FILE}"; echo
          case "${STATUS}" in 2*) exit 0 ;; esac
          if [ "${STATUS}" = "409" ] && [ "${RUN_ID}" = "shakedown" ] && grep -q "unknown run" "${RESPONSE_FILE}"; then
            echo "shakedown accepted: the Worker verified the signature and refused the nonexistent run, which is the expected terminal answer."
            exit 0
          fi
          echo "score report rejected (HTTP ${STATUS})" >&2
          exit 1
